# 引き継ぎ — NeNe Nib / 2026-09-23

## 設計リナから（セッション開始時点・2026-09-23）

**main は `f17dc9e`**（2026-09-22 夜の区切り・[引き継ぎ 09-22](2026-09-22.md) の「夜の区切り時点」）。open な Issue は #117（制御文字の `^M` 描画）・#124（README・current.md・日報の粒度）・#128（本節の施主指示の記録）・#129 / #130 / #131（下ごしらえのスクリプト化の chore）。fixture 1339 件・既定の `nib_tests` 13309 checks・ADR は 0038 まで。

## 席ごとのモデルの切り分け（施主指示 2026-09-23・ADR 0038）

hide の指示: **Opus 5 の消費が速い。原因は判断を含まない仕事まで Opus 5 に出していること。** 次のセッションから、背景席（Agent）を立てるときは仕事の種類で `model` を明示する。正本は [ADR 0038](../adr/0038-model-per-seat-and-scripted-preparation.md)。要点:

- 裁定・受理・採点・依頼書と仕様の文言・物語・保護対象の判断 → **設計席自身**（Fable）。
- 実装（1 task = 1 branch = 1 PR）と差し戻し対応 → **Opus 5**（`model: opus`）。
- 現物調査メモ・棚卸し・PNG や出力の比較・実機用 checkout の準備・ペルソナの意見・報告との突き合わせ → **Sonnet 5**（`model: sonnet`）。メモは 300 行以内・疑問 15 件以内。ペルソナは 4 席まで各 150 行以内。
- テスト一覧の照合・差分の列挙・保護対象の差分 0 確認 → **Haiku 4.5**（`model: haiku`）。
- 判断基準は「間違えたとき誰が直すか」。設計席が照合して直せる仕事は下のモデル。差し戻し 2 回目以降は指示を狭く（変更 1 命題・再実行 1 集合）。ローカルの Ollama は Agent から呼べない（別ランナーの補助席で XS / S の task に限り、受理は設計席）。
- **同じ手順を 2 回以上モデルに踏ませたら 3 回目は `eng/` のスクリプト**（chore を実装席に発行・モデル代ゼロ）。スクリプト化した手順は設計席が直接実行し、受理はスクリプトの実出力を読む。最初の候補: #129（実機用 Release の作成と SHA の記録・起動はしない）・#130（保護対象の差分 0 確認・fixture の逐語再利用・scope ごとの checks 数の前後比較）・#131（verify-window の PNG 保存と前後の画素比較）。
- 指示の語の Nib への読み替え（`tools/` → `eng/`、golden / replay → fixture の逐語再利用と checks 数、mailbox → 無し、launcher → `Start-Process` は設計席）は ADR 0038 の表。
- 依頼書（scratchpad）の冒頭に「席・モデル」の行を足す。日報で「どの席をどのモデルで立てたか」を振り返る。

**次の順（設計リナの案・hide 未確認）**: #129 → #130 → #131（いずれも実装席・Opus・小さい chore）→ #124 docs（下ごしらえは Sonnet で棚卸し → 設計席が書く）→ `incsearch`（未起票・推しは既定オン）→ `VimStep.cpp` / `NibTests.cpp` の分割（ADR）→ #117 → add バッファの chunk 化（ADR）→ マクロ（ADR）。

**環境**: 起動中の exe は `build/release-main-123/NeNeNib.exe`（main `f9e4704`・PID 32532・hide の実機確認用）。worktree は無し。実機の目視は Claude デスクトップの別セッション（computer-use・目視リナ・調査だけ）で行う。

### 手 3: 実装席（Opus 5）の形を変える（施主指示 2026-09-23・ADR 0039・Issue #136）

上の 2 つが手 1（席ごとのモデル）と手 2（繰り返しはスクリプト）。hide の追加指示: **9/22 の transcript（`~/.claude/projects/<repo>/**/*.jsonl` の assistant usage）を集計すると、減っているのは Opus 5 の枠で、消費は全部が Agent の実装席。1 席 150〜400 ターン・文脈 350〜800K・cache read 0.4〜1.7 億トークン。費用は「ターン数 × 文脈長」の積み上げで、太った席を長く走らせるほど二乗で効く。手 1 では減らない。** 正本は [ADR 0039](../adr/0039-implementation-seat-per-step-and-small-tool-output.md)、依頼書の型は [`docs/templates/implementation-seat-brief.md`](../templates/implementation-seat-brief.md)。**次に立てる Opus の実装席から適用する。** 要点:

- 3-1 **1 task を 1 席で走らせない**。probe → 実装 → 差し戻し対応をそれぞれ新しい Agent にし、引き継ぎは報告ファイル（scratchpad の `probe-<issue>.md` / `done-<issue>-<工程>.md`・#141）で渡す。Opus の席を `SendMessage` で次の仕事に使い回さない。`fork` は親の全文脈を継ぐので背景席に使わない。429 の「続きを」は同じ工程の中だけ。
- 3-2 **道具出力を小さく**。テストは対象だけ実行して失敗行だけ tail、ログは `out/` へ落として grep、ファイルは範囲だけ読む。最終報告は 30 行以内で、詳細は報告ファイル、親にはパスと数字だけ。依頼書の型に入れた。
- 3-3 **task は S 級 3 本**。差し戻しは「変更 1 命題 = 新しい席」。
- 3-4 **手 1 を守る**。9/22 の Opus 起動 108 本のうち約半分がレビュー・ペルソナ・棚卸し・撮影・checkout 準備だった。これは Sonnet / Haiku。
- 3-5 **Opus 5.5 は試行**。実装席で 1 日走らせ、枠の減りを見てから決める。`model: opus` がどの版に解決されるかは transcript の `model` の欄で確かめる。
- **計測して言う**。枠の減りを疑ったら usage（cache_read / output / 最大文脈 / ターン数）を席ごとに集計してから手を選ぶ。数字で言えないうちは手を増やさない。集計のスクリプト化（`eng/`）は 2 回目で候補（未起票）。

## 設計リナから（区切り時点・2026-09-23・Issue #134）

**main は `bfa91f0`**（#132・ADR 0038）に本日報の PR が乗る。open な Issue は #117・#124・#129（進行中）・#130・#131。production・fixture・ゲートは今日は変えていない（fixture 1339 件・既定の `nib_tests` 13309 checks）。

**#129 の再開手順**: ブランチ `chore/129-build-release-script`（本体ツリー・origin/main `bfa91f0` の上にスクリプトの commit `1473b2e`）と draft [PR #133](https://github.com/hideyukiMORI/nene-nib/pull/133)。実装席（Opus）が検証・文書・PR 本文まで進めているが、この引き継ぎの時点で完了報告は未受領。再開時は `gh pr view 133` と `git log origin/main..origin/chore/129-build-release-script` で進みを見て、(1) PR 本文の 4 項目が同じ行に値を持つか `gh pr view --json body` で確かめ、(2) 本日報が `docs/reports/2026-09-23.md` を先に作っているので、#129 側が同じファイルを作っていれば `git rebase origin/main` で両方の節を残す形に解いてから、Ready → CI → squash merge。受理はスクリプトの実出力（`out/release/<短い SHA>.json`）を読む形で。

**次の順（設計リナの案・hide 未確認）**: #129 の受理 → #130（機械作業席の代わりになるスクリプト・実装席 Opus）→ #131（PNG 保存・実装席 Opus）→ #124 docs（棚卸しは下ごしらえ席 Sonnet・書くのは設計席）→ `incsearch`（未起票・推しは既定オン）→ `VimStep.cpp` / `NibTests.cpp` の分割（ADR）→ #117 → add バッファの chunk 化（ADR）→ マクロ（ADR）。

**運用の約束（追記）**: (10) Agent は `model` を必ず明示する（ADR 0038 の表）。(11) 小さい chore は依頼書を別に書かず、Issue 本文と ADR を正本にして席とモデルを 1 行で伝える。(12) 実装席が本体ツリーで作業中の docs は worktree（`../NeNeNib-docs`）で書く。(13) 実装席は工程（probe / 実装 / 差し戻し対応）ごとに新しい席にし、`SendMessage` で使い回さず `fork` を使わない。依頼書は `docs/templates/implementation-seat-brief.md` の型で、最終報告は 30 行以内（ADR 0039・手 3）。

**環境**: 起動中の exe は `build/release-main-123/NeNeNib.exe`（main `f9e4704`・PID 32532）。worktree `NeNeNib-docs` は本日報の merge 後に削除する。実機の目視は Claude デスクトップの別セッション（computer-use）。

## Issue #129 — 実機用 Release の作成と SHA の記録をスクリプトに（実装リナから）

統合単位は draft [PR #133](https://github.com/hideyukiMORI/nene-nib/pull/133)。**draft のまま**（Ready・CI・merge・CLAUDE.md 6 節の更新は設計席）。ブランチ `chore/129-build-release-script` は origin/main `bfa91f0` の上に 2 commit（スクリプト＋conformance / 文書）。本体の作業ツリーで作業し、clean で終わっている。起動中の `build/release-main-123/NeNeNib.exe`（PID 32532）と既定の `build/` には触れていない。

**使い方**: `pwsh -NoProfile -File eng/build-release.ps1 -Ref main`。`build/release-<短い SHA>/NeNeNib.exe` と `out/release/<短い SHA>.json`（SHA-256・ref・commit・HEAD・configure / build / total の秒・道具の版）ができる。**起動はしない**ので、設計席が `Start-Process build/release-<短い SHA>/NeNeNib.exe` で開く。ref 無し・無い ref・dirty な作業ツリーは何もせず終了 1（QLT-013）。ref が HEAD と違うときは `build/worktree-<短い SHA>` を一時的に作って build し、終わったら消す。

**引き継ぐ 3 点。**

1. **SHA は「この 1 回の生成物の印」**である。手で作った `build/release-main-123/NeNeNib.exe` と 978944 バイト中 2 バイト（offset `0x80` の PE `TimeDateStamp`）だけが違い、残りは完全一致した。`/Brepro` は入れていないので、同じ commit を作り直すと SHA はこの 2 バイトぶん変わる。「同じ commit なら同じ SHA」を要求するなら別 Issue（`/Brepro` は link の設定なので ADR 相当）。
2. **ゲートには載せていない**（Issue のとおり・QLT-013）。壊れたことは次に実機用の Release を作るときにしか分からない。引数の検査だけは `tests/conformance` が正例 1・反例 3 で守っている（`python eng/test-conformance.py` 163 tests）。
3. **作業機に残した成果物**: `build/release-bfa91f0/NeNeNib.exe`（main `bfa91f0`・SHA-256 `DE4D3900…59AFC`）と `build/release-1473b2e/NeNeNib.exe`（本ブランチの実装 commit）、記録は `out/release/bfa91f0.json` / `out/release/1473b2e.json`、ログは `out/issue129-main.log` / `out/issue129-head.log`（`out/` と `build/` は追跡外）。hide の実機確認に使うなら `build/release-bfa91f0` が main の版である。

**後続**: #130（保護対象の差分 0 確認・fixture の逐語再利用・scope ごとの checks 数の前後比較）と #131（verify-window の PNG 保存と前後の画素比較）が同じ「2 回踏んだ手順をスクリプトに」の残り 2 本。

## 設計リナから（2 回目の区切り時点・2026-09-23・Issue #144）

**main は `0cab39c`**（#130・PR #143）に本節の PR が乗る。open な Issue は #117（`^M` の描画）と #140（verify-window の `--keys` の鍵が届かない揺れ・原因未調査）。worktree は無し。起動中の exe は無し。fixture 1339 件・既定の `nib_tests` 13309 checks・ADR は 0039 まで・conformance 185 tests。

**今日入った道具（設計席が直接使う・モデル代ゼロ）**:
- `pwsh -NoProfile -File eng/build-release.ps1 -Ref main` → `build/release-<短い SHA>/NeNeNib.exe`・`out/release/<短い SHA>.json`（#129・起動は `Start-Process`）。
- `python eng/protected-diff.py --base <ref> [--head <ref>] [--allow <scope> ...] [--build]` → `out/protected/<短い SHA>.json`。PR の受理でこの実出力を読む（fixture の削除・変更が 0・checks の変化が `--allow` の scope だけ）。fixture を足した PR は増える scope を `--allow` で名指しする（#130）。
- `python eng/verify-window.py --capture <dir> [--keys "ihello<Esc>"]` → PNG と `frames.json`、`python eng/compare-frames.py before.png after.png --regions frames.json --expect body,title,status`。設計席は PNG を Read で見る（#131）。鍵が届かなかった回は `changed: false`・終了 1（#140）。
- `python eng/usage-report.py [--since YYYY-MM-DD] [--until YYYY-MM-DD]` → 席ごと（jsonl 1 本 = 1 席）の turns・max_context・cache_read・seat_tokens の表と model ごとの合計、`out/usage/<since>.json`。席の大きさを言うときはこの実出力を貼る（#146）。

**手 3 の初日**: 席は Sonnet 3・Opus 5、どれも 1 工程で新しい席、最終報告 30 行以内、1 席最大 131K トークン（日報の総括）。依頼書は `docs/templates/implementation-seat-brief.md` の型で scratchpad に書き、受け入れ条件の訂正は Issue のコメントに残した（#130 / #131）。

**次の順（設計リナの案・hide 未確認）**: `incsearch`（未起票・推しは既定オン・ADR）→ `VimStep.cpp` / `NibTests.cpp` の分割（ADR）→ #117 → #140（下ごしらえ席 Sonnet で原因の probe → 実装席）→ add バッファの chunk 化（ADR）→ マクロ（ADR）。usage の集計スクリプト（手 3 の「計測して言う」の道具）は hide の返事があれば起票。

**運用の約束（追記）**: (14) PR は draft で作って Ready にする（CI は `ready_for_review` / `synchronize` / `edited` でしか走らない。draft を経ずに作った PR は本文を編集して走らせる）。(15) PR 本文の GIT-004 の欄（`確認する退行:` `対象・依存:` `検証結果:` `再利用:`）は欄名と同じ行に値を書く。(16) 受理は道具の実出力（`out/protected/*.json`・`out/release/*.json`・PNG）を読む。
