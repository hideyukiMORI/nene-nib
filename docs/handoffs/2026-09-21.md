# 引き継ぎ — NeNe Nib / 2026-09-21

## Issue #81: 行単位VISUAL yank

統合単位は [PR #83](https://github.com/hideyukiMORI/nene-nib/pull/83)。必須checkはGit規約・検証記録・空白を確認し、成功した対象検証は再利用する。

hideの続行指示。main `3b02f13`（#77 / PR #82）から `fix/81-visual-yank-caret` で修正。統合状態はGitHubが正。

変更したproductionは `VimStep.cpp::yanked_caret` だけ。行単位VISUALの現在行が範囲の最終行と同じなら範囲先頭の列1へ、それより前なら現在位置へ返す。これで下向き・単一行・同じ行の逆向き選択は列1、上向きの複数行は現在列となり、通常モードへ戻る際の行末の寄せは既存controllerを使う。NORMALのyankと文字単位は変更しない。新しい状態・型・効果は不要。

固定Vimの新規fixture22件と#77の `$2Vy` 1件を再利用し、追加23件・計512件。selector `--vim-visual-yank` は新規23件と直接の既存境界10件のみを実行する。修正前265中13失敗、修正後265すべて成功。Debug/tidy/ASan/UBSan、symbols2libs/0、conformance0、format成功。実測は `out/issue81-oracle/`、unit/build等は `out/issue81-*.log`。詳細は [gate-proofs 5-t](../quality/gate-proofs.md)。成功済み検証は関連入力不変なので繰り返さない。

分割normalのprobeはstartofline有無の単一行/下向き/上向き10件を採用した。同じVim起動内の末尾2件（回数付きV）は、前ケース後のVISUAL記憶を隔離しておらず、独立fixtureより広い範囲になったため判断根拠から除外した。回数付きVとoの期待値はfresh起動のcanonical measure()を正とする。Vimソースは読んでいない。

実機用computer-useはimport後、`sky.list_apps()`がnative pipe接続エラー（os error 2）。**今回の実機確認は未実施**で、アプリの起動・入力も行っていない。旧版PID41000を維持。修正版は `build/issue81/NeNeNib.exe`、SHA-256 `d02d7d3af67b0a89c3821c61a857c121d57815d48aeedf78b72203d26d741c82`。CMakeの一時出力先は既定へ復元済み。`build/NeNeNib.exe`は起動中の旧版なので最新版と取り違えない。

ADR 0018を補足。保存schema・依存変更なし、Waivers: none。root単体で自己レビューし、局所修正のため独立レビューなし。次はIssue #81の統合状態を確認し、残るVimのdot・全文検索・テキストオブジェクト等を焦点Issueごとに進める。
