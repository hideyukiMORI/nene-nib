# 引き継ぎ書 — NeNe Nib / 2026-09-18 終了時点

## 最初に読む情報

- Issue #1 / #3 / #5 / #7 / #9 / #11 / #13 / #14 / #16 / #19 / #20 / #22 / #24 / #26 / #28 / #30 / #31 / #36 / **#43 / #44 / #47** は main へ統合済み（[日報 09-18](../reports/2026-09-18.md)・[09-17](../reports/2026-09-17.md)）。main は `f3b132d`。
- **draft PR は残っていない。** 開いている Issue は無い（この引き継ぎ書の #50 を除く）。次回の最初の作業は **C1**（カラーテーマ・core だけ）か **Vim の 3 本目**（VISUAL）。どちらも Issue を切るところから
- 現在のタスクの要約は [docs/todo/current.md](../todo/current.md)。GitHub Issue が正
- 施主の指示書は `.handoff\START-HERE.md`（git 除外）。仕様は [SPECIFICATION.md](../../SPECIFICATION.md)（D1〜D16）。設計判断は ADR 0001〜0016
- 前日までの引き継ぎ書 [2026-09-17](2026-09-17.md)・[09-16](2026-09-16.md)・[09-15](2026-09-15.md) の「働き方」「実装の要点」「地雷」はそのまま有効。ここには差分だけ書く

## 働き方（差分）

- 実装リナは同時に複数人でよい。**同じ作業ツリーでブランチを切れるのは 1 人**なので、2 人目以降は Agent ツールの `isolation: worktree`（`.claude/worktrees/agent-*`）。Python だけの Issue（eng / docs）は worktree 向き。C++ を触る Issue は build と実機の基準値がある元のチェックアウトで
- worktree のフルゲートは新規ビルド込みで約 6 分で動く。**フルゲートは同時に 1 つだけ**（ベンチが揺れる）。実装リナには「フルゲートの前で止まって報告」を約束させ、設計リナが並行の処理を止めてから「回してよい」と返す（今日は揺れ 0 回）
- worktree の実装リナが終わったら: そのブランチをレビュー → 設計リナが worktree の中でコミット・フルゲート・push → PR → merge → `git worktree remove --force <path>` → 残ったディレクトリを `Remove-Item` → `worktree-agent-*` のブランチを消す。worktree が `main` を checkout したままだと元のチェックアウトで `git checkout main` ができない
- 実装リナが「設計の前提が oracle と食い違う」と報告したら、**出典を当たってから判断する**（今日は `:help ins-special-special` で決定 5 を維持した。oracle の `:normal!` の限界と対話の Vim の振る舞いは別）
- `gh pr create` は draft で作って `gh pr ready` にするか、Ready で作ったら閉じて開き直す（trigger に `opened` が無い）。push と同じ秒に Ready にすると run が起きない
- commit の subject は 100 文字以内（GIT-003）。長い説明は本文へ

## 次回の作業順

1. `git status --short --branch` で main が clean であることを確認する（`f3b132d` 以降）
2. **C1**（`docs/plans/2026-09-15-colorschemes.md` 第 2・4・5 節）: `core::Theme` / `SyntaxPalette` / `derive_ui` / 組み込み 9 テーマの `constexpr` の表・名前の重複なし・全トークンが埋まっている・コントラスト比 4.5 以上の単体テスト。UI は `Palette` だけを使い続ける。`Palette::title_bar`（D16）の列を表に足すところから。ADR が 1 つ要る（テーマの模型・導出関数・出典と許諾の書き方）。見た目の判断（各テーマの UI トークンが絵として妥当か）は C3 で `/design` の絵にして hide が見る
3. **Vim の 3 本目**: VISUAL（`v V`。`Ctrl-v` は `virtcol` が要るので後）を「範囲を決める」（`motion_range`）の 1 本に選択として渡す形・`o`・Esc・`d c y` on VISUAL・画面の高さを engine に渡す形（`vim_step` の引数か `VimState` か）を決めてから PgUp / PgDn・`Ctrl-d/u`・`H M L`。fixture を足して `python eng/vim-oracle.py --regenerate`（Vim 9.1）。ADR 0017 相当
4. 小さい残件（別 Issue 候補・生きた経路ではない）: `keys_trial` の残る例外の口（`dialog_closed` の `assert`・`process.wait` の `TimeoutExpired`）、`--check --values` が #24 より前の記録で `KeyError`、通常モードの Ctrl+V の貼り付けの改行の正規化
5. CI の他の指紋の基準値: artifact `speed-records` が指紋ごとに 5〜7 run たまったら `eng/perf-reference.json` に項を足す（ADR 0016 の決定 4。中央値の中央値・出典は `note`）

```powershell
git status --short --branch
pwsh -NoProfile -File ./eng/check.ps1        # 約 2 分 30 秒（worktree の新規ビルドなら約 6 分）。画面中央を空ける・並行の処理をしない
python eng/verify-window.py                   # Debug の exe。IME の節が IME の開閉を変えて戻す。キーボードに触らない
python eng/measure-speed.py --record          # 値と起動の内訳
python eng/vim-oracle.py --regenerate         # Vim 9.1 が要る。CNF-010 が生成物の SHA を見る
```

## 確認済みと未確認の境界

- main `f3b132d` のフルゲート（#43）は設計リナ・実装リナとも終了 0。分岐 92.41%・`nib_tests` 2335 checks・`Speed: 5 benches checked, 0 regression(s), 0 unmeasurable`。CI 成功（run 35360937741・EPYC 7763・CI でも判定）
- **CI の速さゲートが実際に判定した run**: PR #48 の attempt 2（run 35357889836）と PR #49（35360937741）。記録だけの run: #48 の attempt 1（9V45）・run 35359066206（9V74）。artifact `speed-records` は 3,058 バイト・90 日
- #36 の 2 つの口: 実機で両方作れて traceback なし（`speed-reference.md` の表）。**欠測を含む実機のフルゲートの姿（終了 2）はまだ実機で見ていない**
- Vim の 2 本目: fixture 195 件の再生・手書き 5 本・実機の `yyp` / `uuu` の画素。**未確認**: `2C`、ハングル・絵文字、全角・タブを含む行をまたぐ `j` `k`（`virtcol`）、実機での `c` の undo（`cwfoo<Esc>u` は単体テストのみ）
- 前日からの未確認（#31 の帯のライトの画素・96 DPI・IME のライト／Google 日本語入力／ATOK・Vim モードのホイールとドラッグ・Ctrl+O・ファイルダイアログ・複数モニタ・Mica 非対応環境・device lost）はそのまま

## 実装の要点（差分）

- Vim（ADR 0015・#43）: `VimState::pending` は `VimPendingOperator{op, count}`、`unnamed_register` は `VimRegister{text, kind}`（本文は LF）。`vim_step` は `motion_range`（`VimMotionRange`: 範囲＋行単位か。`:help exclusive` の言い換えと `op_delete` の行単位化）→ `removed` / `changed` / `yanked` / put。`VimEffect` は 9（`VimPutString{at, utf8, caret}`）。controller: `perform(VimPutString)` が LF → `newline_of(line_ending())` と `caret_after_put`、`vim_boundary()` が INSERT なら `EditBoundary::absorb`。`EditHistory::absorbed(previous, edit)` は公開の純関数（3 形）。`vim_word_end`（Vim の `end_word`・`VimWordEndStop` で `e` と `cw` を分ける）。鍵の表は 25 行・移動の表は 10 行
- oracle（`eng/vim-oracle.py`）: `getregtype('"')` も取り、生成物の列は `name / text / keys / expected / line / column / register / register_kind`。先頭 4 行目に `// fixtures.json: sha256 … / N fixtures`（CNF-010・#44）。`KEY_NAMES` に `<Home>` `<End>`。fixture は 195
- 計測器（#36）: `TrialNotObserved`（`measure-speed.py`）。`keys_trial` は `observed_marks` → 純関数 `marks_of(text)` で読む。`read_report`（起動・16 MiB）は不変
- CI の基準値（ADR 0016・#47）: `eng/perf-reference.json` の `machines.e7a87d5b6ac1e14b`（`note` に出典 7 run）。`--check` の「基準値が無い」の 1 行は指紋と CPU 入り（ASCII の `--`）。`check.ps1` が `Tee-Object` で拾ってまとめにも出す。`check.yml` に `Upload speed records`（`if: always()`）
- CNF-010（#44）: `eng/conformance.py` の `fixture_digest_checks`、検出語は `eng/conformance-rules.json` の `fixtureDigestPattern`、`config-bindings.json` に読み手

## 地雷（追記）

`std::string::find_first_not_of` は `memchr` を core の外へ出す（ARC-003。自前のループで）／`state.pending.value()` は `bugprone-unchecked-optional-access` が拒む（読む口を 1 本にして両方の枝をテストで通す）／fixture に変更が 2 回ある鍵列（`ddpu`）を置かない（`:normal!` 1 回 = 1 単位で落ちる）／矢印で undo の単位が切れることは oracle で測れない（単体テスト）／`.gitattributes` は LF だが `.claude/worktrees` の build は元と別（`build/` を共有しない）／cp932 の机で出る行に U+2014 を置かない（`print` が落ちてゲートが「退行」と言う）／CI の rerun は別の host に当たる／`check.yml` の trigger に `opened` は無い／commit の subject は 100 文字以内。

## Claude Code と証拠の場所

- セッション: 設計リナは同じセッションの続き。実装リナは Agent ツール（Opus 5）で 4 人（#36・#44 worktree・#43・#47 worktree）＋ 調査 1 人（CI の 16 run のログ。scratchpad の `ci-bench/`）。#43 は `SendMessage` で「決定 5 は維持」「rebase」「フルゲート可」を 1 通で伝えて再開
- 速さ: `docs/quality/speed-reference.md`（「欠測と『測れなかった』」に #36 の再現の表・「CI の記録」に 16 run の表）。実機の記録 `out/speed/2026-09-18T*.json`（git 対象外）。CI の記録は artifact `speed-records`（PR #48 以降）
- Vim: `docs/quality/gate-proofs.md` 5-g（#43: 7 点・SHA・画素）。1-a に CNF-010 と QLT-014（CI）の証明行
- ADR 0015（Vim の 2 本目）・ADR 0016（CI の基準値）。`docs/adr/README.md` に両方の行

## 規則・保留

Waivers: none。QLT-014 は施主の実機と CI の指紋 `e7a87d5b`（EPYC 7763）で active、CI の他の指紋と打鍵 2 本は planned（ADR 0016）。ADR 0012 の「生成物と json の一致」は CNF-010 で active（#44）。ADR 0005 / 0012 の fixture の再生は active（レジスタの種類も突き合わせる）。QLT-013 は planned のまま（記録は gate-proofs 5-g / 1-a）。ゲートの緩和・新しい除外・許可シンボル・`eng/architecture.json`・許容・実機の基準値の変更なし。`eng/perf-reference.json` に CI の項 1 つ、`eng/conformance-rules.json` に `fixtureDigestPattern`、`.github/workflows/check.yml` に artifact の step。
