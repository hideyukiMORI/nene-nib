# 引き継ぎ — NeNe Nib / 2026-09-20

## 続行: Issue #79

成果の統合単位は [PR #80](https://github.com/hideyukiMORI/nene-nib/pull/80)。独立レビューは未解決指摘なし。関連実装が不変のため、push/review/mergeではgate-proofs 5-rの成功結果を再利用する。

hideが#76の実機動作を確認し、o/Oを挙げて続行を指示した。main `8f48f49`から `feat/79-vim-open-lines`、[Issue #79](https://github.com/hideyukiMORI/nene-nib/issues/79)、ADR 0028。今回はroot単体で調査・設計・実装・検証し、独立レビューだけ読み取り専用で依頼した。統合状態はGitHubのIssue/PRを正とする。

NORMALのo/Oが1行を開いてINSERTへ入り、3o/3OはEsc時に入力を反復する。既存VimPutStringをVimInsertAtへ移行し、p/Pと開行/反復の改行変換・本文編集を共用。VimInsertRepeatは残り回数とLFの入力だけを持つ。Backspaceの入力外への削除・移動・エンジン外の編集で解除する。EditHistoryの範囲内合成とINSERT退出効果後のsealにより、開行と入力/反復を1回でundoできる。途中の保存/移動は既存どおり単位を区切る。

実Vim40件を新規測定し、既存431件を来歴照合付きで維持して計471件。初回unit551中549成功（新40fixtureは成功）、空ファイルのCRLF期待値と反復サイズguardを430 checksで確認、外部入力境界を46 checksで確認した。native3件・120 DPI・CRLF/LFの保存とundo・日本語/絵文字・bar/blockと追従を確認。Debug/tidy/ASan/UBSan、symbols2libs/0、conformance0、format成功。失敗したテスト側期待値と修正、実行コマンド、再利用根拠は [gate-proofs 5-r](../quality/gate-proofs.md) が正。

証拠は `out/issue79-oracle/`、`out/issue79-*.log`、`out/issue79-native/`。実機exe SHA-256 `75e89f89f9a4f92534979ecf45f5017a37938149b0dbe62b1d21172bca0e2e12`。成功結果を工程・SHA・文書更新だけで再実行しない。保存schema・runtime依存は不変、Waivers: none。

次候補は分離済みIssue #77（VISUAL移行時の希望列）。dot・全文検索・テキストオブジェクト・一般Ex・矩形などは未実装。一般i/a回数とautoindentも今回の範囲外。表現不能な反復サイズは拒否するが、実メモリ予算は未導入。以下は過去時点の記録であり、分担・未実装・停止の記載を現在の指示として読み替えない。

## 続行: Issue #76

成果の統合単位は [PR #78](https://github.com/hideyukiMORI/nene-nib/pull/78)。再開時の統合状態はこのPRとGitHub Issueを正とする。

前件Issue #72はPR #75でmain `2e22281`へ統合済み。hideの続行指示で [Issue #76](https://github.com/hideyukiMORI/nene-nib/issues/76) の `gg` / `G`・指定行移動を `feat/76-vim-line-jumps` で実装・限定検証した。設計はADR 0027。統合状態はGitHubのIssue/PRを正とする。

次キー待ちは `optional<VimInputWait>`（文字検索種別か `VimPrefix`）の一本。明示countはoperator側も含めて判断し、未指定Gだけ最終行。後方へのdocument motion yankだけ共通の到達列を一時wantedへ渡し、既存performed/yankedの経路を使う。取消はmode/selection/wanted/検索記憶を保つ。

新oracle42件と旧389件で431件。`out/line-jump-oracle/`に48件の一度きりのfixture測定・6分割probe・採否理由・生成照合がある。`python -X utf8 out/git/assemble-line-jump-fixtures.py`でcanonical parser/formatterを使い、入力全体と旧行/測定ソース/Vim版/既定設定を照合して生成した。生成のための実Vim再測定は0。生成器自体は不変。

検証は `--vim-line-jumps` 初回989中987成功、新42fixture成功、修正対象 `--vim-line-jump-recovery` 15成功。初回2件は可視本文を全文と比較したテストと、既存VISUAL移行の希望列解除（Issue #77）。成功した製品コードは不変で987を再利用した。native6シナリオ、Debug/tidy/ASan/UBSan、symbols 2libs/0、conformance0、format成功。証拠・追加probeの無効2回を含む経緯はgate-proofs 5-q。成功結果を文書や工程の変更だけで再実行しない。

次候補は [Issue #77](https://github.com/hideyukiMORI/nene-nib/issues/77) の `$v` 希望列。本文 `abcd\nx\nabcdef` の `$vg<Esc>jj` は現行appで3:4、固定Vimで3:7。gなしappの再現値は未測定で、base/currentの `entered_visual` が同一であることから原因を切り分けた。今回のg取消は `v$g<Esc>jj` で確認済み。保存schema・runtime依存は不変、Waivers: none。

## 夜の再開: Issue #72

成果の統合単位は [PR #75](https://github.com/hideyukiMORI/nene-nib/pull/75)。再開時の確定した統合状態はこのPRとGitHub Issueを正とする。

hideの続行指示で20:47 JSTに再開。`feat/72-vim-character-search`でADR 0026の行内検索を実装し、限定検証まで完了した。Astraが設計・判断・統合、SOLがoracle実測とC++実装、LUNAが限定native検証を担当する今回の明示分担。下記の停止指示は再開前の履歴である。

oracleの対象指定は `python eng/vim-oracle.py --regenerate --only char-search- --reuse-ref b6d19ff`。元commitの入力・生成物・測定処理・Vim版を検証して未変更行を維持する。追加60件と旧329件で389件となり、正式生成と旧行維持の確認は成功済み。同じ実装/入力/環境では再測定しない。

検索待ちと検索記憶を分ける設計と、取消/未発見/反復/operator/VISUAL、未設定と空yank済みregisterの区別はADR 0026を正とする。単体621 checks、native7シナリオ、Debug + ASan/UBSan + tidy、symbols 2 libraries / 0 violations、conformance 0 violationsが成功。独立レビューの指摘は解消済み。証拠と限定範囲、検証側の修正はgate-proofs 5-p。

通常の文字入力経路は不変で、検索待ちのIME変換を新たに受け付ける変更はない。全件oracle・無関係なGUI/設定/テーマ/性能を測り直していない。成功結果は入力が不変のpush/review/mergeでも再利用する。保存schema・runtime依存は不変、Waivers: none。次の焦点Issueはまだ選定していない。


## 停止と再開点（03:45 JST・hide指示、Issue #73）

- 製品の最終統合: [Issue #70 / PR #71](https://github.com/hideyukiMORI/nene-nib/pull/71)、main `9a493fa`。required check成功、main同期済み。C1〜C4bのテーマ/設定機能で一区切り。
- 次の [Issue #72](https://github.com/hideyukiMORI/nene-nib/issues/72) は要件を登録しただけ。`f/F/t/T` と `;` / `,`。既存コードとoracle入口の読取りまでで、ADR・実測・fixture・実装・検証は未着手。ブランチも未作成。
- hideから再開指示が来るまで新規開発を進めない。今回の文書確定は通常のPR経路で行う。これより下の「以前の追記」は各時点の履歴であり、未実装/次作業の記載を現在の指示として採用しない。

再開時はclean mainを確認して #72 の焦点ブランチを作る。ADR 0005 / 0012 / 0015 / 0018 / 0019、VimState・VimStep・eng/vim-oracle.pyを読む。行内文字検索の回数、前後/手前、未発見、Esc、`;`/`,` の記憶、d/c/yとVISUALの範囲、UTF-8を本物のVim 9.1で確定してからADRを受理する。Vimソースを読んで写さず、既存のmotion/range/performed経路へ接続する。

現行oracleのCLI `--regenerate` は329件を全て測り直す。今回の差分に無関係な再実測はしない。既存の `measure/run_vim/header` を再利用し、対象指定と成功済み結果の維持を最小限で設計する必要がある。大掛かりな自動選定基盤は作らない。新UIは現時点で予定せず、既存NORMAL/VISUALを使う。

検証はQLT-001 / QLT-012 / ADR 0021が正。所有者・工程・SHA・文書更新だけを理由に再実行しない。C4bの検証済みコマンドと証拠はgate-proofs 5-o、`out/user-theme-verification`、`out/speed/issue70-selected.json`。自走時も単体実行を既定とし、必要な独立レビューだけ読み取り専用で依頼する。

## 最新の追記: C4b（Issue #70）

C4aはPR #69でmainへ統合。C4bはADR 0025のThemeChoice/ThemeCatalogとThemePortを追加し、設定・Ex・Ctrl+Pに同じ利用者テーマを渡す。テーマは起動時に一度、フォルダ直下128件/各16KiBまで読む。不変ThemeDocumentを共有し、通常キーごとに全配色を複製しない。ファイル変更は再起動で反映。

SettingsIssueはSettingsFailure / ThemeLookupFailureの和型。壊れた保存済みテーマは名前を保持して警告し、元settingsはwrite禁止。失敗中の同値設定もportの判断を省略しない。selected_themeは設定所有者からThemeを借り、rvalueの所有者からname/view/records/catalogを取り出せない。描画はframeの設定が所有を維持する。

初期ファイルはEditorController構築のoptional OpenDocumentで通常applyへ通し、その後inventory.noticeを載せる。引数付き起動でnoticeが消えない。一般のOpenDocumentと入力後の診断寿命は変えていない。列挙途中失敗と128件超過は部分集合を採用せず、組み込み＋noticeで起動する。

対象unit88 / Ex153 / palette130 / 設定codec23 / catalog301、native120 DPI（選択/補完/色/本文保持/再起動/破損保護/初期notice）、symbols2libs/0、conformance0、Debug/Release成功。独立レビューの所有・notice・blocked同値指摘を修正済み、未解決なし。必要な検証・再利用の詳細はgate-proofs 5-o。settings.v1のschema・runtime依存・waiver変更なし。

次候補はVimの行内文字検索 f/F/t/T と ;/,。本物のVimでoperator/visual/回数の意味を確定して既存motionへつなぐ。テーマの無変更部分は再実行しない。

## 以前の追記: C4a（Issue #68）

C3bはPR #67でmainへ統合。C4aはADR 0024の利用者テーマ形式・所有・読込まで。アプリからの選択はまだ未実装。coreのThemeName（1〜64 bytes、underscoreを正規化）とThemeDocument/OwnedThemeSourceが名前/出典を所有する。`theme_view`はlvalueだけを借り、rvalueはdeleted。既存Themeと組み込み表は不変。

adaptersのThemeCodecはUTF-8/16KiB/version1、名前一致/予約名、必須metadataと本文16色、任意UI16色、厳密RGB/RGBA、本文/UIのコントラスト4.5を検証。UIはderive_uiの正典。`load_theme`は既存FilePortへ制限付きreadのみ。`KeyValueFields`で設定codecと行分割/空白/BOM/重複拒否を共用した。設定の受理/拒否と保存形式は変えていない。

対象テーマ127 checks、設定codec23 checks、coreの名前/所有21 checks、symbols/conformance成功。名前の18 checksはadapterで成功後にpure core unitへ移し、通常のcore単体にも載るようにした。無変更のadapter読込・既存GUI・性能・Vimの再実行はしない。形式/例はdesign/user-theme-format.md、実測はgate-proofs 5-n。

独立レビューでbasenameと期待名の一致をcaller任せにしていた点を修正。`load_theme(FilePort, FilePath)` が正規ファイル名を検証して期待名を導き、追加のファイル読込19 checksで確認した。修正後のレビューは追加指摘なし。

次はC4b: 起動時カタログ、ユーザーテーマを表せる設定参照、Ex/Ctrl+P候補、対象名付きの拒否理由へ接続する。ThemeDocumentを不変で共有し、EditorStateの各キー更新で全テーマを複製しない設計が必要。SettingsCodecは現在BuiltinTheme専用なので、カタログの検証と従来の読込失敗後の保存禁止を両立させる。既存schemaの互換を保ち、欠落/壊れた利用者テーマを黙って別テーマへ保存し直さない。

## 以前の追記: C3b（Issue #66）

C3aはPR #65でmainへ統合済み。C3bはADR 0023のCtrl+P設定一覧。`EditorState::command_input` は任意の `CommandInput`（ExのCommandLine / CommandPalette）で、同時に二つの入力は持てない。paletteはCommandLineの編集と共通コマンド候補を再利用し、設定は同じ `evaluate_ex` / `persist_settings` を通る。部分列検索、フォント値の補完入力、クリック実行に対応。未知の明示テーマもExへ渡す。

元の本文/選択/Vimモード・count・pending・undoを保ち、Ctrl+Pで閉じる。IME変換中は開かず、入力中はIMEを閉じ、終了後は元の状態へ復元する。配置/可視行/クリックは `PaletteLayout` の純関数を共用。footerは選択位置/総件数を示す。ファイル/フォルダ/ブックマーク/履歴はまだ一覧へ統合しない。

対象unitはCtrl+P 130 checks、共通化したEx153 checks。native120 DPIでテーマ/フォント/クリック/狭窓/元状態/IME/undo/再起動が成功し、追加surface検査で件数表示と未知テーマを確認した。検証・再利用根拠はgate-proofs 5-m。保存形式、外部依存、waiver変更なし。次はC4の利用者テーマ形式と名前の所有を設計する。

## 以前の追記: C3a（Issue #64）

C2はPR #63で統合済み。最新はADR 0022のEx設定コマンド。`EditorState`の `command_line` / `command_message` が正本で、NORMALの既存Vim表→`VimOpenCommandLine`から開く。`CommandLine`はUTF-8一行256 bytes、`evaluate_ex`は保存候補/表示またはtyped failure。テーマ表、FontSize解析、`persist_settings`を既存経路と共用する。

UIはステータス左を入力/結果に置換し、右項目を維持。長い入力はclipとcaret追従。本文caretは入力中非表示。入力キーは専用intentへ流し、Ctrl+AltはCtrl shortcutと見なさない。status中の隠れたtoggleへクリックを渡さず、取消後のcaret配置はbody内だけ。IMEはNORMALの閉状態。Ctrl+P・一般Ex・履歴・範囲・`:w`/`:q`は未実装。

検証済み: Debug/Release、`nib_tests --ex-settings`153 checks、`nib_adapter_tests --settings-codec`23 checks、symbols2libs/0、追加STL許可の正例/反例、`verify-ex.py`と`--only input-guards`（120 DPI）、起動/打鍵の限定測定（退行/欠測0）。詳細はgate-proofs 5-l。関連入力が不変なら再実行不要。C2の不変部分の検証も再利用する。

次はC3bのCtrl+P共通一覧を設計し、通常/Vim両方の設定選択を同じ評価と保存へつなぐ。ファイル/履歴の統合は前提の状態所有を設計して別の焦点Issueで進める。利用者テーマはC4。新規runtime依存・waiver・保存schema変更なし。

## 直近の変更

Issue #60（C2・ADR 0020）。本文設定の所有者は `EditorState::settings`、controllerの唯一の入口は `EditorPorts`。設定形式/場所/競合検査は adapters/win32、バイト置換は既存 FilePort。`FontSize` は8〜40 pt、DIPと既定比率は名前付き純関数。本文の配置・クリックは4引数の `body_layout` を共用する。

- `settings.v1` は version / colorscheme / font_family / font_size がそれぞれ必須。UTF-8、4096 bytes上限、未知版・欠落・重複・未知キー・不正値は拒否。欠落だけが既定値での正常起動。
- `Win32SettingsAdapter` は最後に読んだ/書いたbytesを控え、lockを共有無しで開き、比較してから書く。読込失敗後は保存を拒否し元ファイルを保つ。外部エディタの比較後の競合は保証外。
- 保存成功後にだけ設定を変更する。設定失敗は文書失敗と別。起動時エラーは VisibleLines 等で消さず、窓が起動時/設定操作時だけ告知する。
- Ctrl+`+` / `-` / `0` と Ctrl+wheel は通常/Vim共通。上限・下限・既定復帰のno-opと保存失敗は表示高通知を送らず、ユーザーのスクロール位置を保つ。
- `EditorFrame` は設定/フォント名を所有する。rendererは描画資源のキャッシュだけを持ち、描画とクリック前に設定へ同期する。OS→テーマは `theme_for` の1経路。

## 検証と再利用

Debug/Release、単体3,992 checks、adapter109 checks、symbols 2 libraries / 0 violations、coverage91.56%、focused nativeと起動/打鍵性能が成功。詳細とコマンドは [gate-proofs](../quality/gate-proofs.md) の Issue #60 節と [日報](../reports/2026-09-20.md)。入力が不変なら push・レビュー・merge・担当交代で再実行しない。

新方針は #61 / PR #62 / ADR 0021。古い日報の「最終HEADでフルゲート」「全件を二人が実行」を継承しない。全件コマンドは理由付き `-Full -Reason` の例外用途。調査・設計・実装・検証は単体、必要な独立レビューのみ読み取り専用。

## 次と未確認

C3は Ex入力と`:colorscheme`・`:set fontsize` / `guifont`を、C2の設定所有/保存へ接続する。Ctrl+Pも同じテーマ表・設定意図を使う。既存の編集デザイン正本を読み、比較案が必要なら可視化する。FR-016 / FR-017 はC2だけで完了にしない。

未確認は別モニターへ移動するDPI変更、変換中の拡縮に伴う候補窓の目視、他IME。120 DPIの変換/確定と単体96/120/192 DPI配置は検証済み。未インストールのフォントはDirectWriteの既存fallback、専用フォント選択UIは未実装。起動・打鍵性能の結果を16 MiB入出力の測定結果とは扱わない。Waivers: none。

C4bの起動/打鍵4指標も5試行で退行0・欠測0。初回描画209.9973ms、窓32.813ms、単打鍵1.14ms、200打鍵2.627ms（通常の空profile）。証拠はout/speed/issue70-selected.json、gate-proofs 5-o。
