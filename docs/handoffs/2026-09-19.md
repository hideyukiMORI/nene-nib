# 引き継ぎ書 — NeNe Nib / 2026-09-19 未明の作業の終了時点

> 後続の作業は [2026-09-19 夜の引き継ぎ](2026-09-19-night.md)。以下は未明時点の記録として残す。

## 最初に読む情報

- Issue #1 / #3 / #5 / #7 / #9 / #11 / #13 / #14 / #16 / #19 / #20 / #22 / #24 / #26 / #28 / #30 / #31 / #36 / #43 / #44 / #47 / **#52 / #53** は main へ統合済み（[日報 09-19](../reports/2026-09-19.md)・[09-18](../reports/2026-09-18.md)）。main は `a322323`。
- **draft PR は残っていない。** 開いている Issue は無い（この引き継ぎ書の #56 を除く）。次回の最初の作業は **hide に 3 候補の順を聞く**（下）。設計リナの推しは Vim の 4 本目
- 現在のタスクの要約は [docs/todo/current.md](../todo/current.md)。GitHub Issue が正
- 施主の指示書は `.handoff\START-HERE.md`（git 除外）。仕様は [SPECIFICATION.md](../../SPECIFICATION.md)（D1〜D16）。設計判断は ADR 0001〜0018
- 前日までの引き継ぎ書 [2026-09-18](2026-09-18.md)・[09-17](2026-09-17.md)・[09-16](2026-09-16.md)・[09-15](2026-09-15.md) の「働き方」「実装の要点」「地雷」はそのまま有効。ここには差分だけ書く

## 働き方（差分）

- **2 人の実装リナを同時に走らせてよい**（C++ を触る Issue も worktree で可。configure から Debug ビルドまで数分）。ただし**フルゲートは同時に 1 つ**。両方に「フルゲート前で止まって報告」を約束させ、設計リナが「どちらも止まっている」か「相手がコンパイルしていない隙」に 1 つずつ回す。設計リナ自身が回すなら実装リナのゲートは省いてよい（今日は 2 本ともそうした）
- worktree の枝を rebase すると `docs/adr/README.md` が衝突する（両方が行を足す）。番号順に両方残して `git add` → `GIT_EDITOR=true git rebase --continue`
- 実装リナが「設計が oracle と食い違う」と報告したら**出典（`:help`・仕様）を当ててから判断**。前日は設計が正しく（決定 5・`:normal!` の限界）、今日は実装リナが正しかった（決定 7・`3v`）。ADR の決定文を直すのは設計リナ
- 見た目の判断（D12）は `/design` スキルがこのセッションに無いので、`docs/design/look/build_boards.py` で PNG を作って artifact で見せる形にする（C3 で要る）
- 施主に聞くのは「施主決定を覆す・依存を足す・ゲートを弱める・oracle の設定を足す・見た目の判断」。今日は無かった。**次回は設定の保存場所・形式・版（C2）が施主の好みなので先に聞く**

## 次回の作業順

1. `git status --short --branch` で main が clean であることを確認する（`a322323` 以降）
2. **hide に順を聞く**: (a) Vim の 4 本目 (b) C2 (c) C3。推しは a → b → c
3. **Vim の 4 本目**（Issue から。ADR 0019 相当）: 画面の高さが要る移動（PgUp / PgDn・`Ctrl-d/u`・`H M L`）は engine に「見えている行数と先頭行」を渡す形（`vim_step` の引数か `VimViewport` の値）と、`VimEffect` に `VimScrollTo` が要るかを決める。**先に oracle の `-es` で `set lines=N` が効くかを実測**（効かなければ H M L と Ctrl-f/b は手書きテストで守り、ADR に「正直に」）。`Ctrl-v`（矩形）は `virtcol`（全角・タブの表示幅）が要るので、`j` `k` の欲しい列を code point → 表示幅に直す判断と一緒に。ドラッグで VISUAL（FR-003）は anchor が `EditorState` にあるので、ui/win32 のドラッグが選択を置いて `visual` に切り替えるだけ
4. **C2**（設定の保存・ADR）: 置き場 `%LOCALAPPDATA%\NeNeNib\`・形式（JSON か独自か）・版 `settings.v1`・テーマ名・フォントサイズ（D14: 8〜40 pt・既定 13.5・Ctrl+`+`/`-`/`0`・Ctrl+ホイール）・読めない版は型のある失敗（ARC-009）。**設定全体（テーマ・フォント・鍵）の形を 1 回で決める**（ADR 0008 の決定 8）。adapters がファイルを読み書きする（ARC-003）
5. **C3**（`:colorscheme`）: Vim の Ex コマンドライン（`:` のモード・ステータスバーの左に入力・D15 案 A）が先。`theme_named` と `builtin_themes` の表を補完に使う。導出した 7 テーマの見た目を `build_boards.py` の絵にして hide が見る（`ime` / `search` の規則の見直しはここ）
6. 小さい残件（別 Issue 候補）: VISUAL 中の Ctrl+X でモードが残る・CPP-004 に `.value()` の tidy の癖を 1 行・`docs/adr/README.md` の行の漏れを CNF-006 で見る・前日からの 3 つ

```powershell
git status --short --branch
pwsh -NoProfile -File ./eng/check.ps1        # 約 2 分 30 秒（worktree の新規ビルドなら約 6 分）。画面中央を空ける・並行の処理をしない
python eng/verify-window.py                   # Debug の exe。Vim の節に V → d が入った。キーボードに触らない
python eng/vim-oracle.py --regenerate         # Vim 9.1 が要る。CNF-010 が生成物の SHA を見る
```

## 確認済みと未確認の境界

- main `a322323` のフルゲート（#53）は設計リナが worktree の新規ビルドで終了 0。分岐 91.29%・`nib_tests` 2862 checks・`Speed: 5 benches checked, 0 regression(s), 0 unmeasurable`。CI 成功（run 35369296225・EPYC 7763・判定）
- #52（C1）: 設計リナのフルゲート終了 0・`verify-window.py` 終了 0（画素の表明は不変）。CI 成功（run 35368235341・9V74・記録だけ）
- **未確認**: 導出した 7 テーマと採用案の本文トークンの見た目（C3 の絵まで誰も見ていない）。`VISUAL LINE` のステータスバーの幅。VISUAL の `p u ~ > <` 等が「何もしない」ことは手書きテストだけ。前日からの未確認（`2C`・ハングル／絵文字・`virtcol`・帯のライトの画素・96 DPI・IME のライト・ATOK・ホイール・Ctrl+O・複数モニタ・Mica 非対応・device lost）はそのまま

## 実装の要点（差分）

- テーマ（ADR 0017・#52）: `Theme` / `SyntaxPalette`（`operators`）/ `ThemeSource` / `UiMixWeights`（明暗 1 行ずつの百分率）/ `ThemeDerivation.hpp` の `derive_ui`（`mix` は整数・`on_accent_for` は明るさ 160 の境目・`panel` は「前景へ寄せてから白へ」の 1 式）/ `BuiltinThemes.hpp`（`std::array<Theme, 9> builtin_themes`・`theme_of` は添字引きで `static_assert` 9 本・`theme_named` は `same_theme_name` で `_` ≡ `-`）。`BuiltinTheme` は 9・`palette_of` は削除・`palette_for(Appearance)` は `theme_of(theme_for(appearance)).ui`
- VISUAL（ADR 0018・#53）: `VimMode` 4 値・`VimSelect{Selection}`・`vim_step(state, text, const Selection&, key)`・`vim_visual_range(text, selection, mode) → VimMotionRange`（`VimVisualRange.cpp`・CMakeLists に追加）・`VimStep.cpp` の `rested_in`（モードで寄せるか）・`removed_exactly`（`op_delete` の行単位化を通さない）・`entered_visual` / `widened`（回数ぶん広げる）・`visual_acted`（25 動作を `default` 無しで網羅）・`moved_step`（移動 10 分岐を 1 本に）。controller: `perform(VimSelect)`・`highlighted_range()`・`settle_vim_caret` / `vim_boundary` / `composition_ignored` / `caret_shape_for` の VISUAL。`ModeLabel` `VISUAL` / `VISUAL LINE`。`EditorWindow::ime_blocked` は VISUAL でも切る。表は 28 行（`v V o`）
- fixture 256（`--regenerate` 3 回一致）。`verify-window.py` の Vim の節: `i` → `hello` → Esc → `0x` → `yyp` → `V` → `d` → `u` × 4

## 地雷（追記）

`docs/adr/README.md` に ADR の行を足し忘れても検査は落ちない（#43 で 0015 が漏れた。ADR を書いたら README の行も同じコミットで）／worktree の rebase は README で衝突する（番号順に両方）／`bugprone-unchecked-optional-access` は `has_value()` の直後でない `.value()` も落とす（`std::optional` 同士の `==` で比較）／`SyntaxPalette` の `operator` は予約語（`operators`）／VISUAL の fixture に `p u ~ > < J r I A gv X D C Y` を置かない（この縦切りでは何もしないので oracle と食い違う）／`3v` `3V` は回数ぶん選ぶ（草稿は誤りだった）／`v$d` は改行まで消す／`op_delete` の行単位化は VISUAL に掛からない／C1 のゲートを VISUAL の実装リナがコンパイルしていない隙に回した（プロセス一覧で `clang|cmake|ninja` が無いのを見てから）。

## Claude Code と証拠の場所

- セッション: 設計リナは同じセッションの続き（09-18 から通し）。実装リナは Agent ツール（Opus 5）で 2 人（#52 元の作業ツリー・#53 worktree `.claude/worktrees/agent-*`。終わったら `git worktree remove --force` → ディレクトリを削除 → `worktree-agent-*` の枝を削除）
- テーマ: `docs/adr/0017-*.md` の「正直に」（solarized-light・埋めた役割・採用案の本文トークン・`ime` / `search` の差・tidy の癖）。9 テーマの出典 URL は `src/core/BuiltinThemes.hpp` の `ThemeSource`
- Vim: `docs/quality/gate-proofs.md` 5-g「Vim の 3 本目・VISUAL」（SHA・61 件・oracle に合わせた 3 点・測れないもの・カバレッジ・実機の窓）。ADR 0018 の「正直に」
- 速さの記録: `out/speed/2026-09-18T1[5-7]*.json`（git 対象外。C1 と VISUAL のゲート）

## 規則・保留

Waivers: none。QLT-014 は施主の実機と CI の指紋 `e7a87d5b` で active（ADR 0016）。ADR 0017 の強制（コントラスト比 4.5・名前の重複なし・全トークン）は active（`nib_unit`）、ui/win32 の色リテラルの字句検査は planned（別 Issue）。ADR 0018 の強制（fixture の再生・表示と操作の範囲の一致）は active。QLT-013 は planned のまま。ゲートの緩和・新しい除外・許可シンボル・`eng/architecture.json`・許容・基準値の変更なし。`CMakeLists.txt` に `src/core/VimVisualRange.cpp`（core の source 1 つ）。
