# 引き継ぎ書 — NeNe Nib / 2026-09-17 終了時点

## 最初に読む情報

- Issue #1 / #3 / #5 / #7 / #9 / #11 / #13 / #14 / #16 / #19 / #20 / #22 は main へ統合済み（[日報 09-17](../reports/2026-09-17.md)・[09-16](../reports/2026-09-16.md)・[09-15](../reports/2026-09-15.md)）。main は `f35439c`。
- 現在のタスクの要約は [docs/todo/current.md](../todo/current.md)。GitHub Issue が正。未着手の Issue: #24（起動の `D3D11CreateDevice` 160 ms）。
- 施主の指示書は `.handoff\START-HERE.md`（git 除外）。仕様は [SPECIFICATION.md](../../SPECIFICATION.md)（D1〜D15）。設計判断は ADR 0001〜0012。
- 前日の引き継ぎ書 [2026-09-16](2026-09-16.md) と [2026-09-15](2026-09-15.md) の「働き方」「実装の要点」「地雷」はそのまま有効。ここには差分だけ書く。

## 働き方（差分）

- 設計リナは依頼書を scratchpad に書き、実装リナ（Opus 5・Agent ツール）に渡す。実装リナは commit / push / PR をしない。設計リナがレビュー → commit → draft PR → **自分でもフルゲート** → PR 本文を GIT-004 の形で → Ready → CI → squash merge。今日はこの流れを 2 本とも同じ作業ツリーで回した（並行の worktree は使っていない）
- **PR 本文は GIT-004 の必須項目（`目的:` / `使った正典経路:` / `規則 ID:` / `検証` / `Waivers:` / `残るリスク:` と `Closes #N`）を CI が文字列で見る。** PR #18 / #23 / #25 の本文が形の見本
- 設計リナが docs を、実装リナが src / tests / eng を同じ作業ツリーで同時に編集してよいが、**改行や整形の後処理を相手のファイルに掛けない**
- 施主に聞くのは、施主決定を覆す・依存を足す・ゲートを弱める・oracle の設定を足す判断だけ（今日は `backspace` の 1 件。推しを添えて「推しで」で確定）

## 次回の作業順

1. `git status --short --branch` で main が clean であることを確認する
2. **Issue #24**（起動 160 ms）: 段 1 は実験 4 つを `measure-speed.py --record` の内訳で比べて `speed-reference.md` に記録するだけ（製品コードは実験後に戻す）。実装リナに依頼書を渡す。段 2 は結果を見て設計リナが ADR（30 ms 以上縮む実験があれば採用・無ければ「窓を先に見せる」案 A。`startup-first-frame` の定義は変えず `window_shown` の節目を第 2 の値として足す）
3. **IME（FR-012）**: 変換中の表示は採用案（`ime` のトークン）。`ImmGetCompositionStringW` を adapters か ui のどちらに置くか・変換中は Vim の鍵を奪わない（`WM_IME_COMPOSITION` の間は `VimKeyPress` を出さない）を ADR 0013 に。Vim モードが在るので設計できる
4. **Vim の 2 本目**: オペレータ側の回数・INSERT 中の Backspace の undo 単位・Home / End・`c y p P`・VISUAL。fixture を `tests/vim/fixtures.json` に足して `python eng/vim-oracle.py --regenerate`。生成物と json の一致の検査（SHA-256 を生成物に書く）もここで
5. CI の基準値: CI のベンチの値が 3〜5 回たまったら `eng/perf-reference.json` に CI の指紋の項を足す Issue（PR #18 / #23 / #25 で 3 回分のログがある）

```powershell
git status --short --branch
pwsh -NoProfile -File ./eng/check.ps1        # 約 2 分 30 秒。ベンチを含むので 1 本ずつ
python eng/verify-window.py                   # Debug の exe。窓が出る。キーボードに触らない
python eng/measure-speed.py --record          # 値と起動の内訳を見るだけ
python eng/vim-oracle.py --regenerate         # Vim 9.1 が要る。2 回走らせて生成物が同じことを見る
```

## 確認済みと未確認の境界

- フルゲート（#25 の HEAD `5ff3a2a` → squash `f35439c`）は終了 0。分岐 93.50%。`Speed: 4 benches checked, 0 regression(s)`。CI も同じ HEAD で成功
- 実機で確認済み: Vim へトグル → `i` → `hello` → Esc → `0x` → `u` × 2（画素）。起動の内訳（実機と CI）
- **未確認**: Vim モードでの日本語の打鍵（実機の IME は無いので `WM_CHAR` の多バイトは fixture だけ）。Vim モードのホイールとドラッグ。Home / End / PgUp / PgDn は Vim では効かない（仕様どおりまだ）。Ctrl+O / ファイルダイアログ・「保存しますか」・96 DPI・複数モニタ・Mica 非対応環境・ライトの外観・device lost は前日のまま未確認

## 実装の要点（差分）

- Vim: `core::vim_step(VimState, TextBuffer, Offset, VimKey) → {VimState, VimEffect}`。表は `VimStep.cpp` の `normal_bindings`（16 行）と `motion_bindings`（8 行）。語の移動は `VimWordMotion.cpp`（文字の種類の表）、寄せは `VimCaret.cpp`（`vim_resting_caret` / `vim_first_non_blank`）。効果 8 つは `EditorController::perform` が写し、`settle_vim_caret` が NORMAL のキャレットを文字の上へ寄せる（鍵・クリック・Ctrl+矢印のあと）。INSERT の出入りで `EditHistory::sealed()`
- 窓: `EditorWindow` は `frame.mode` を `mode_` に控え、Vim のとき `type_character` → `VimCharacter`、`press_vim_key` → `KeyVimSpecial` の表（7 行）。Tab は文字の鍵。Ctrl+Z / Y は Vim では送らない
- fixture: `tests/vim/fixtures.json`（`name` / `text` / `keys` / `settings`）→ `eng/vim-oracle.py --regenerate` → `tests/vim/VimFixtures.hpp`（`// clang-format off`・`constexpr std::array<VimFixture, 87>`）。`keys` は `ihello<Esc>3jx` の記法で、末尾は NORMAL で終わること（スクリプトが `<Esc>` を 1 つ足して結果が同じか検査する）。`text` は改行で終わらせない
- 起動の節目: `Milestone` の先頭 8 つが起動の正典順。`measure-speed.py` の `STARTUP_MILESTONES` と `NibTests.cpp` の表と同じ綴り。`processCreationToOriginMs` が最初の区間

## 地雷（追記）

PR 本文は GIT-004 の形（文字列で検査）／`where vim` は Git の 9.0（oracle は Program Files の 9.1 をフルパスで）／`:normal!` は 1 回がまるごと 1 つの undo 単位なので undo の fixture は「1 回の変更 → `u`」だけ／失敗する鍵は fixture の列の最後にだけ置く／fixture の `text` は改行で終わらせない・CRLF は流せない／同じ作業ツリーで並行編集するとき改行・整形の後処理を相手のファイルに掛けない／Issue #19 より前の exe を `measure-speed.py` で測ると `first_reading` の `AssertionError`（古い `build-release` を消す）。

## Claude Code と証拠の場所

- セッション: 設計リナは前日と同じセッションの続き（context の要約をまたぐ）。実装リナは Agent ツール（Opus 5）。#22 は 1 つのエージェントに直しまで続けて頼んだ（SendMessage で再開）
- 起動の内訳の記録は `out/speed/*.json`（git 対象外）と `docs/quality/speed-reference.md`。CI の内訳は Actions のログの `breakdown` の行
- Vim の oracle の記録は `docs/quality/gate-proofs.md` 5-g。fixture の正本は `tests/vim/fixtures.json`

## 規則・保留

Waivers: none。ADR 0005 の強制は planned → active（fixture の再生が CTest に載った）。ADR 0012 の「生成物と json の一致の検査」は planned。QLT-014 は実機で active・CI は記録だけ（変更なし）。ゲートの緩和・新しい除外・許可シンボル・`eng/architecture.json` の変更なし。`eng/tool-versions.json` に `"vim": "9.1"`、`eng/config-bindings.json` に `tests/vim/fixtures.json` を結線。
