# 引き継ぎ — NeNe Nib / 2026-09-22

## 設計リナから（2 回目のセッションの区切り時点・2026-09-22）

**main は `8c76e25`**（#101 `eb79415` → #100 `b07c574` → #98 `a44f380` → #91 `433bb9a` → #92 `9e41f79` → #99 `220fe76` → #108 `8c76e25`）。open な Issue は #85（literal CR）・#106（cp932 の端末の既存失敗）・#111（後ろ向き選択の引用符の対）・#112（矩形 VISUAL・進行中）・#114（本総括）。fixture は 1137 件（正準形・CNF-011）、既定の `nib_tests` は 11553 checks。ADR は 0034 まで受理、0035 は提案。

**#112 矩形 VISUAL の再開手順**: worktree `C:/Users/info/WORKS/NeNeNib-112`・ブランチ `feat/112-vim-visual-block`（origin/main `8c76e25` の上に ADR 0035 の commit `bf4969c`）。実装リナに依頼済みで、依頼書（scratchpad・セッション限り）の正本は Issue #112 の受け入れ条件と ADR 0035 の決定 1〜10。置き方: `VimMode::visual_block`・`VimSpecialKey::control_v`・`VimRegisterKind::block` と幅・`VimVisualExtent` に `VimBlockExtent`／行ごとの範囲は `vim_block_range` 1 本で描画（`span_of`）・Ctrl+C/X・`d x y r`・`.`・貼付が共用／効果は `VimRemoveBlock` `VimReplaceBlock` `VimInsertBlock` の 3 つで controller が undo 1 単位／Ctrl+V の写しは Vim の NORMAL / VISUAL だけ。fixture の接頭辞は `block-`、gate-proofs は 5-af。draft PR まで実装リナ、Ready・CI・merge は設計リナ。途中で止まっていたら `git status` と `git log origin/main..HEAD` で進みを見て、commit 済みの所から続ける。

**次の順（設計リナの案・hide 未確認）**: #112 矩形 → #111 引用符の対 → #85 literal CR（oracle の読み込みと改行模型の ADR が要る）→ #106 cp932。Issue 未起票: 矩形の `I A c C > <`、`> < gu gU J` のオペレータ、`ip ap it at`、一般 Ex（`:w :q`）、複数タブ・Ctrl+P のファイル統合、INSERT の矢印を仮想桁に（ADR 0034 の結果）。

**運用の約束（追記）**: 前回の約束に加えて、(1) 2 人目の実装リナは worktree（`git worktree add ../NeNeNib-<issue> -b <branch> origin/main`）で、設計リナが ADR を先に commit してから渡す。後から merge する側が `git rebase origin/main` し、fixtures.json は再生成の書き戻しで解く。(2) worktree の build は `-G Ninja` を明示し、`symbols.py` / `conformance.py --build-dir` は `build/.cmake/api/v1/query/codemodel-v2` が要る。(3) gate-proofs の節記号は 5-ae まで使用（5-x は空き）。(4) 実装リナが本体ツリーで作業中、設計リナは本体ツリーで作業ツリーを動かす git を打たない。(5) Ready の前に PR 本文の 4 項目と `docs/adr/README.md` の状態の行を設計リナが見る。(6) 429 で止まった実装リナは、上限が戻ったあと同じ agent に「続きを」と送れば再開する。

**環境**: computer-use の native pipe は繋がらない。起動中の旧版 PID は保持。exe は `build/issue100/NeNeNib.exe`（検索の入力行を含む最新の窓）・`build/issue108-release/NeNeNib.exe`（速さ用）で `build/NeNeNib.exe` は旧版。worktree は `NeNeNib-112` だけが残っている（`NeNeNib-91` / `-92` / `-108` は merge 後に削除済み）。

## Issue #106 — 検査の入出力を端末符号化から切り離す（実装リナから）

統合単位は [PR #119](https://github.com/hideyukiMORI/nene-nib/pull/119)。**draft のまま**（Ready・merge・CLAUDE.md 6 節の更新は設計リナが行う）。ブランチ `fix/106-cp932-utf8-io` は origin/main `54b3a21` の上に 2 commit（実装・文書）。本体の作業ツリーで作業し、worktree `NeNeNib-85` には触れていない。作業ツリーは clean。

**次に触る人への注意が 3 つある。**(1) **符号化を固定する場所は 2 か所だけ**。python は `tests/conformance/test_verification_policy.py` の `run_tool`（`encoding="utf-8"` ＋ `errors="replace"` ＋ 子の `PYTHONUTF8=1`）、pwsh は `eng/validate-git.ps1` の前置き（`[Console]::OutputEncoding` と `$OutputEncoding`）。`subprocess.run` を新しく書くときは `run_tool` を通す。(2) **`errors="replace"` は消さない**。`eng/check.ps1` の QLT-001 の throw は toolchain を読む前に出るので、pwsh のエラー装飾（cp932 の省略記号や罫線）はコンソールの code page のまま流れてくる。厳密に読むと reader thread が落ちて `stdout` が `None` になる（#94 の観測）。(3) **cp932 の症状は端末の設定で姿が変わる**。`PYTHONUTF8` の有無で落ちるテストが入れ替わり、UTF-8 の端末では再現しない。反例を取るときは `chcp 932` ＋ `[Console]::OutputEncoding` を 932 にした子 pwsh から回す（今回はその実行台を scratchpad に置いた。セッション限り）。

**残り**: `tests/conformance/test_vim_oracle.py` の `subprocess` は Issue #85 が同じファイルを触っているので今回は寄せていない。#85 の統合後に `run_tool` に寄せるかを設計リナが決める（`vim_oracle.subprocess.run` を patch する作りなので端末符号化には依らず、急がない）。

## Issue #112 — 矩形 VISUAL（`Ctrl-v`・実装リナから）

統合単位は [PR #118](https://github.com/hideyukiMORI/nene-nib/pull/118)。**draft のまま**（Ready・merge・CLAUDE.md 6 節の更新は設計リナが行う）。ブランチ `feat/112-vim-visual-block` は origin/main `87d8d71`（#111 統合後）の上に ADR 0035 の 1 commit ＋ 実装 3 commit。作業ツリーは clean。作業は worktree `C:\Users\info\WORKS\NeNeNib-112` の中だけで行い、本体の `NeNeNib` には触れていない（`out/issue112-oracle/` の probe の読み取りを除く）。

**合流の形**: 途中で origin/main が `75bf047`（#114 の総括）→ `87d8d71`（#111）と進んだので、実装 commit のあとに `git rebase origin/main` を 1 回掛けた（衝突なし）。fixtures.json は rebase のあとに追記し直し、全件再生成 → `--only block-` の順で締めてある。

**次に触る人への注意が 5 つある。**(1) **矩形の範囲は `vim_block_range` と `vim_replayed_block_range` の 2 つの入口があり、行ごとの形を作るのは `built_block` 1 本**。前者は選択の 2 つの角から、後者は `.` の記録した幅から左右の桁を決める。`.` の再生で角を読み直すと短い行へ畳まれたぶん矩形が痩せるので、`VimState.replayed_block` が立っているあいだは必ず後者を通す。(2) **`VimBlockLine` の 4 つの空白は役割が違う**。`lead` / `tail` はレジスタに入る空白（端に掛かった文字の**内側**の桁と、行が矩形より手前で終わるときの幅ぶんの埋め）、`keep_lead` / `keep_tail` は削除・置換のあと本文に残す空白（**外側**の桁）。内外が逆なのは Vim のもので、`probe5.py` / `probe6.py` が 34 ケースで測ってある。(3) **`r` は桁の数だけ書く**（`min(right, 行の桁数) - left + 1`）。文字の数で書くと Tab の上で合わない。(4) **矩形の編集は controller が 1 つの `Edit` に畳む**。行ごとに `replace` を呼ぶと `absorbed()` が繋がらず undo が行数ぶんに割れる。(5) **矩形の `c I A C > < J ~`・VISUAL の中の `p`・テキストオブジェクト・`gv`・`virtualedit` は範囲外**で、鍵は取り消して矩形のまま残る（固定 Vim は動く）。`c` は `visual_changed`、テキストオブジェクトは `visual_acted` の `insert_object` の枝で止めてある。

**この worktree の注意**: build は `build/issue112/`（Debug・`-G Ninja` を明示し、`.cmake/api/v1/query/codemodel-v2` を手で作ってから configure した）。速さ用の Release は `build/issue112-release/`（`NeNeNib.exe` の SHA-256 は `eabf9c734727aa7d6be1ec32a0eff7538792989f9573e472b44ba1a7d960a674`、Debug の exe は `6cf32bb4ffddd0e2967d6ff9c36e8a7b132de5517c2eb108bf2a5db50b42251c`）。probe と fixture 追加の道具は `out/issue112-oracle/`（`out/` は追跡外なので作業機にだけある）。**oracle の全件再生成は 1 回 20 分ほど**かかるので、`KEY_NAMES` に触るときは時間を見ておく。

## 設計リナから（1 回目のセッションの区切り時点・2026-09-22）

**main は `8f2c363`**（#84 `82f260d` → #87 `d26c232` → #88 `b887e1b` → #94 `aefcd9a` → #93 `e6dfc14` → #97 `8f2c363` → #101）。open な Issue は #85（literal CR）・#91（VISUAL の `.`）・#92（入力記録の二重経路）・#98（fixtures.json の整形）・#99（テキストオブジェクト残差）・#100（検索・進行中）。fixture は 842 件、既定の `nib_tests` は 8823 checks。

**#100 検索の再開手順**: ブランチ `feat/100-vim-search`（origin/main `e6dfc14` の上に ADR 0032 の commit と補足 2 commit）。draft PR #103。production は未着手で作業ツリーは clean。probe の証拠は `out/issue100-oracle/probe{,2,3,4}.{py,json,txt}`（`out/` は追跡外なので作業機にだけある）。**最初の一手は ADR 0032 補足の「決定 1 の追記」**: `EditorFrame.command_line` が `core::CommandLine` そのものを持ち `command_line_of()` が `const CommandLine &` を返す契約なので、`SearchLine` を足す前に描画と窓が使う入力行の見え方を「プロンプト文字・文字列・caret」の小さな値 1 つに畳む（振る舞いを変えない整えの commit）。次に照合器 `src/core/VimPattern*` と走査 `vim_search` を書いて `--vim-search` の unit で固め（走査規則は補足の「決定 4 の追記」をそのまま）、`SearchLine` → `VimOpenSearch` → `VimKey` の `VimSearchPattern` → VimStep の表 → controller の Submit → renderer のプロンプトの順に繋ぐ。fixture は `--only search-` の部分再生成で、候補は #93 の `add-fixtures.py` の要領で「区切った形」と「1 回の形」の両方で測って食い違いを拒否する。限定検証は依頼書（scratchpad の brief-100.md はセッション限りなので、Issue #100 の検証計画と ADR 0032 を正本にする）。main は `8f2c363` へ進んでいる（#97 が `tests/unit/NibTests.cpp` の selector 表と既定実行の表を変えた）ので、再開時に `git rebase origin/main` してから selector を **両方の表の末尾**に足す。

**次の順（設計リナの案・hide 未確認）**: #100 検索 → #98 整形（検索の fixture 追記の後に 1 回だけ整形し直すため、検索の merge 後）→ #91 VISUAL の `.` → #99 テキストオブジェクト残差 → `Ctrl-v`（virtcol が要る）→ #92 → #85。`> < gu gU J` のオペレータ、`ip ap it at`、一般 Ex（`:w :q`）、複数タブ・Ctrl+P のファイル統合は Issue 未起票。

**運用の約束（今日決めた・守った）**: 設計リナが Issue と ADR（提案）をブランチに commit してから実装リナへ依頼書（scratchpad・セッション限り）で渡す。実装リナは実測 → 食い違いは期待値を合わせず報告 → 実装 → 限定検証 → 文書 → draft PR まで。Ready・CI・merge・CLAUDE.md 6 節の更新は設計リナ。ADR の「状態」は実装リナが実測後に受理へ更新してよいが、決定を直したときは「文脈」に実測との差を残す。

**環境**: computer-use の native pipe は前日から繋がらない。起動中の旧版 PID は保持。`build/issue84|87/NeNeNib.exe` は各 Issue の成果物で `build/NeNeNib.exe` は旧版。#93 と #97 は engine・tests だけの変更で exe を作っていない。CMake の出力先は既定に復元済み。

## Issue #111: 後ろ向きのVISUAL選択の引用符の対

統合単位は [PR #116](https://github.com/hideyukiMORI/nene-nib/pull/116)。**draft のまま**（Ready・merge・CLAUDE.md 6 節の更新は設計リナが行う）。ブランチ `feat/111-vim-quote-object-backward` は origin/main `75bf047` の上に 2 commit（実装＋fixture と契約 → 文書）。本体の作業ツリー `C:\Users\info\WORKS\NeNeNib` で作業し、作業ツリーは clean。worktree `NeNeNib-112`（#112）には触れていない。

**いまの状態**: `vim_text_object_range` は 1 本のまま。引用符だけ `quote_outcome` という枝に分かれ、対の選び方が 3 本（`collapsed_quote_pair` / `forward_quote_pair` / `backward_quote_pair`）、VISUAL の置き方が `quote_selection` 1 本にある。括弧は `paired_outcome`（「新しい範囲が今の選択より広くなければ回数を 1 つ増やす」）のままで、引用符はその枝を使わず Vim と同じ「選択がちょうど内側なら引用符ごと」（`inside_quotes`）に置き換えた。ADR 0031 に「補足（2026-09-22・Issue #111）」を足し、決定 4・決定 6 と「残る穴」の文を直した。

**次に触る人への注意が 3 つある。**(1) **対の選び方は「畳んだ位置 / 前向き / 後ろ向き」で違う**。同じ桁でも `6l di"` は隙間 ` c ` を取り、`7l vh i"` は手前の `bb` を取る。直すときは 3 本すべてに同じ実測があるかを確かめる（`out/issue111-oracle/probe.py` の掃引と `model.py` が 645 ケースを 1 つの模型で説明している）。(2) **anchor はその場に残ることがある**ので、`VimTextObjectSpan` の範囲と選択は一致しない前提で読む。(3) **caret が引用符の上かどうかは「生の文字」で見る**（`\"` の引用符も上に載っていると数える）一方、対の番号と偶奇は escape を飛ばした表で数える。この非対称は Vim のもので、`esc-*` の掃引で測ってある。

**残る穴**: `it at` / `is as` / `ip ap` は後続のまま。引用符については 645 ケースで閉じており、既知の食い違いは無い。

**検証**: `--vim-text-objects` 2232 / `--vim-dot` 1593、unit 全体 11873 checks、`ctest` 4 件成功（`nib_unit` 2.47 s）、symbols 2 libs / 0、conformance 0、format 成功。build は Debug / tidy / ASan / UBSan（初回だけ `readability-function-size` で落ちて関数を 1 本分けた）。速さ・Release・画面確認・`check.ps1 -Full` は実行していない（engine の意味論だけの差分・QLT-001 / ADR 0021）。fixture は 1176 件、入力 SHA-256 `5cae75d18203f861d928001a1e2b85be1526e60981a677bdf5f57a29db84b54e`。exe は作っていない。

## Issue #99: テキストオブジェクトの残る3点

統合単位は [PR #110](https://github.com/hideyukiMORI/nene-nib/pull/110)。**draft のまま**（Ready・merge・CLAUDE.md 6 節の更新は設計リナが行う）。ブランチ `feat/99-vim-text-object-residuals` は origin/main `9e41f79`（#92 統合後）の上に rebase 済みで 2 commit（実装＋fixture と契約 → 文書）。本体の作業ツリー `C:\Users\info\WORKS\NeNeNib` で作業し、作業ツリーは clean。

**いまの状態**: `vim_text_object_range` は 1 本のまま、返り値が `VimTextObjectOutcome = std::variant<VimTextObjectSpan, VimTextObjectCancel>`（新しい 3 つのヘッダ）。`VimTextObjectSpan` は「オペレータが使う範囲」と「VISUAL が置く選択」を持ち、両端の置き方（括弧は必ず前向き・引用符は向きのまま・語は後ろ向きの選択では anchor を動かさない）まで範囲関数が決める。`VimTextObjectCancel` は取消のあとに残る `Selection` を持ち、engine が `VimMoveTo` / `VimSelect` / `VimNoEffect` に写す。後ろ向きの走査は `VimWordMotion` に足した `vim_word_object_begin`（`bck_word` の stop = TRUE）と `vim_word_object_previous_end`（`bckend_word` の eol = TRUE）で、前向きの `vim_word_object_end` / `vim_word_stop_forward` と同じ表を引く。ADR 0031 に補足を足し、「合わせていない点」と「残る穴」の該当文を消した。

**実測**: 208 ケース（`out/issue99-oracle/probe{,2,3}.{py,json,txt}`。`out/` は追跡外なので作業機にだけある）。(b) は実装が既に正しく、決定 7 のままで一致した。fixture は 35 件（候補 35・食い違い 0）で 1055 → **1090 件**、入力 SHA-256 `e4c933becb6ac8a8ce3125af3a9b8405d4c417c2956373a56fb7a4bae36287ef`。初回の再生で 35 件すべてが実装と一致。

**残る穴**: 後ろ向きの選択で**引用符のどの対を選ぶか**は、行の引用符が奇数のときと対と対の隙間に caret があるときだけ Vim と違う（`a "bb" c "dd" e` の 7 桁から `vh i"`）。前向き・畳んだ位置・対の中に居る後ろ向きは一致している。ADR 0031 の「残る穴」に 1 文だけ残し、fixture を採っていない。Issue は未起票。

**検証**: `--vim-text-objects` 1912 / `--vim-dot` 1593 / `--vim-visual-wanted` 208 / `--vim-visual-yank` 265 / `--coverage-negative` 22、unit 全体 11130（返り値の型を変えたので 1 回）、`nib_unit` 2.32 s、symbols 2 libs / 0、conformance 0、format 成功。build は Debug / tidy / ASan / UBSan で初回から成功。速さ・Release・`check.ps1 -Full` は実行していない（差分は入力と編集の経路で描画とファイルに触れないため・QLT-001 / ADR 0021）。exe は `build/issue99/NeNeNib.exe`（`build/NeNeNib.exe` は旧版なので取り違えない）。CMake の一時出力先は既定へ復元済み。

**後続**: `it at` / `is as` / `ip ap`、`Ctrl-v`、`> < gu gU` のオペレータ、上の引用符の対の穴。

## Issue #108: Vimの桁を仮想桁（virtcol）で数える

統合単位は [PR #113](https://github.com/hideyukiMORI/nene-nib/pull/113)。**draftのまま**（Ready・merge・CLAUDE.md 6 節の更新は設計リナが行う）。ブランチ `feat/108-vim-virtual-column` は origin/main `220fe76`（#99 統合後）の上に ADR 0034 の 1 commit ＋ 実装 3 commit。作業ツリーは clean。作業は worktree `C:\Users\info\WORKS\NeNeNib-108` の中だけで行い、本体の `NeNeNib` には触れていない。

**いまの状態**: 表示幅の表（`DisplayWidthRange.hpp` の 491 行・固定 Vim 9.1 の `strdisplaywidth()` の全 code point 実測）と仮想桁の純関数 5 本が core にあり、Vim の桁の規則を使う経路だけがそれを通る。`VimWantedColumn.column`（`j` `k` `H M L` `Ctrl-d/u/f/b` `PgUp/PgDn`）と `VimCharacterExtent.column`（VISUAL の `.`）が `VirtualColumn` になり、着地は `offset_at_virtual_column` 1 本。`TextPosition`・テキストオブジェクト・`f t`・`r`・検索・描画は `Column`（code point）のまま。ADR 0034 は受理、ADR 0033 の「結果」の穴の文は閉じたことを書いて置き換えた。fixture は `virtcol-` の 47 件を足して 1137 件、SHA-256 `0b0756ba9406f285d2939252bd0effac7e2485b7f757b07a922bf657479df80a`。

**次に触る人への注意が 4 つある。**(1) **桁の関数は 3 本で役割が違う**。`virtual_column`（最初の桁）・`virtual_column_end`（最後の桁）・`caret_virtual_column`（Vim がキャレットを描く桁＝ Tab だけ最後の桁）。欲しい列と `.` の再生の起点は必ず `caret_virtual_column`、VISUAL の記録の先頭は `virtual_column`、末尾は `virtual_column_end`。この非対称は Vim のもので、`out/issue108-oracle/probe4.py` / `probe5.py` が 21 ケースで測ってある。(2) **逆引きは行より右の桁で `line_end`（Vim が NUL を置く桁）を返す**。最後の文字で止めると短い行へ畳まれた `.` が改行を巻き込まなくなる（#91 の実測が変わる）。NORMAL で 1 文字戻すのは `rested_in` の仕事のまま。(3) **表は Vim の実測が正本**で、Unicode の表を写したものではない。Vim を上げたら `probe2.py` を回して差分を見る。行を足すときは昇順と非重なりを保つ（`static_assert` が落とす）。(4) **`Ctrl-v` はこの上に載る**。矩形の左右の桁は `VirtualColumn` で、`VimVisualExtent` に 3 つめの値を足す形（ADR 0033 の「結果」）。

**検証**: `--vim-virtual-column` 424 checks（新規）、`--vim-visual-wanted` 208 / `--vim-line-jumps` 989 / `--vim-line-jump-recovery` 15 / `--vim-dot` 1593 / `--vim-text-objects` 1912 / `--vim-search` 1375、unit 全体 11553 checks（#99 の 11130 から）、`ctest` 4 件成功（`nib_unit` 2.74 s）、symbols 2 libs / 0、conformance 0、format 成功。**速さは明示実行して 0 regression**（1 打鍵 0.936 ms / 16 MiB 270.950 ms / 起動 214.929 ms / 窓 34.531 ms / 200 打鍵 2.702 ms・基準値は変えていない）。詳細は [gate-proofs 5-ae](../quality/gate-proofs.md)。実機の画面確認は未実施。

**この worktree の注意**: build は `build/issue108/`（Debug・`-G Ninja` を明示し、`.cmake/api/v1/query/codemodel-v2` を手で作ってから configure した）。速さ用の Release は `build/issue108-release/`。`eng/measure-speed.py` は `--bench` を渡しても `--check` では 5 本すべてを測る。probe と fixture 追加の道具は `out/issue108-oracle/`（`out/` は追跡外なので作業機にだけある）。`probe2.py` の全 code point 掃引は 1 回 30 分弱かかる。

## Issue #92: 割り込みをengineの1本の経路へ

統合単位は [PR #109](https://github.com/hideyukiMORI/nene-nib/pull/109)。**draftのまま**（Ready・merge・CLAUDE.md 6 節の更新は設計リナが行う）。ブランチ `refactor/92-vim-interrupt-one-path` は origin/main `433bb9a`（#91 統合後）の上に 3 commit（実装 → 契約 → 文書）。作業ツリーは clean。作業は worktree `C:\Users\info\WORKS\NeNeNib-92` の中だけで行い、本体の `NeNeNib`（別の実装リナが #99 を進行中）には触れていない。

**いまの状態**: engine の純関数 `vim_interrupted` が「外から割り込まれたときに捨てるもの」を決め、controller の `interrupt_vim_insert` はそれを呼んで履歴の単位を切るだけ。捨てる範囲は入力行の取消と共通の `input_discarded` 1 か所にあり、取消（`vim_cancelled_input`）はそれに欲しい列とモードの休止を足したもの。ADR は書いていない（ADR 0030 の「結果」の 1 文だけ更新）。振る舞いは変えておらず、fixture も足していない。

**合流の形**: base は origin/main `433bb9a` で、#99（本体の worktree で進行中）とはファイルが重なる可能性がある（`tests/unit/NibTests.cpp` の scope 表の近く・`docs/todo/current.md` / `docs/quality/gate-proofs.md` / `docs/reports` / `docs/handoffs`）。#99 が先に main へ入ったら、Ready にする前に `git rebase origin/main` して契約の追加位置（`verify_vim_open_line_external_scope` と `verify_vim_open_line_contracts`）と gate-proofs の節記号（5-ac）を確かめ直す必要がある。production の 2 ファイル（`VimStep.cpp` / `EditorController.cpp`）は #99 の範囲と重ならない見込み。

**検証**: production の変更前後を同じ worktree の build で測り比べ、`--vim-open-line-external` 46 / `--vim-open-line-recovery` 430 / `--vim-dot` 1593 / `--vim-search` 1375 / `--vim-character-search` 621・unit 全体 10833 checks が同数で成功。契約を足したあとは 54 / 573 / 10841 checks。`ctest` 4 件成功（`nib_unit` 2.33 s）、symbols 2 libs / 0、conformance 0（字句・build graph とも）、format 指摘なし。カバレッジ・速さ・Release・全件は実行していない（engine の純関数 1 本と controller の 1 関数と契約 1 つだけの差分・QLT-001 / QLT-012）。詳細は [gate-proofs 5-ac](../quality/gate-proofs.md)。ログは `out/issue92-*.log`（`out/` は追跡外なので作業機にだけある）。

**次に触る人への注意**: `interrupt_vim_insert` の `mode == insert` の guard は意図して残してある（外すと NORMAL の保留オペレータ・回数まで割り込みで消え、履歴も毎回切れて振る舞いが変わる）。割り込みで捨てるものを増やすときは `input_discarded` の 1 か所を直せばよく、入力行の取消にも同じだけ効く。この worktree の build は `build/issue92/`（`-G Ninja` と `.cmake/api/v1/query/codemodel-v2` を手で用意した）で、exe は作っていない。

## Issue #91: VISUALで行った変更の`.`

統合単位は [PR #107](https://github.com/hideyukiMORI/nene-nib/pull/107)。**draftのまま**（Ready・merge・CLAUDE.md 6 節の更新は設計リナが行う）。ブランチ `feat/91-vim-visual-dot` は origin/main `a44f380`（#98 統合後）の上に ADR 0033 の 1 commit ＋ 実装 3 commit。作業は worktree `C:\Users\info\WORKS\NeNeNib-91` の中だけで行い、本体の `NeNeNib` には触れていない。

ADR 0033 を実測のあとで**受理**にし、決定 5・6 を実測に合わせて直した。engine は `VimRepeatRecord.visual` と `VimReplay.reselect`（どちらも `optional<VimVisualExtent>`）を足しただけで、記録の分岐は `vim_step` の後段の 1 か所のまま。選び直しは core の純関数 `vim_visual_reselect` 1 本で、controller は `VimSelect` と同じ写しで選択を置いてから鍵を流す。

**要判断は 1 点**: 固定 Vim は桁を**仮想桁**で数えるが、この engine の `Column` は code point で `wanted_column` から `H M L` まで一貫している。仮想桁は表示幅の表と tabstop（＝ `Ctrl-v` と同じ前提）が要るので、#91 では合わせずに差として残した（幅の揃った本文では一致する。Tab と幅の混在は fixture に採らず unit が engine の答えを固定）。**`virtcol` の Issue を `Ctrl-v` と一緒に起票してほしい。**

そのほかに実測で直したのは、`N.` が VISUAL の記録では回数を使わないこと、1 行は桁の個数・複数行は最終行の絶対桁・`$` は「行末まで」のままであること、畳まれた再生のあとも記録が縮まないこと。最後の 1 つは「再生中か」を状態に持たずに済むよう、`.` の step が大きさだけの記録を `recording` に種として置く形にした。

fixture は候補 82 件のうち 4 件を `add-fixtures.py` が機械的に拒否して 78 件を採用し、977 → 1055 件。`--only visual-dot-` の部分再生成で 78 件だけを測り、初回の再生で全件一致した。1055 件の入力 SHA-256 は `fd673b1a3f9bfd93c8628049bd058558ea3aeaac1d9a5a048401d79b62ba135d`。

`--vim-dot` 1593 checks、共有境界 6 scope（変更前と同数）、unit 全体 10833 checks、`ctest -R nib_unit` 2.28 s、build / symbols 2libs/0 / conformance 0 / format がすべて成功。詳細は [gate-proofs 5-ab](../quality/gate-proofs.md)。

**この worktree の注意**: `cmake -S . -B build` は generator を明示しないと Visual Studio を選んで `CXX=clang-cl` を無視する（本体は既存の cache が Ninja だったので気づかない）。`-G Ninja` を足して configure した。`eng/symbols.py` / `conformance.py` は `build/.cmake/api/v1/query/codemodel-v2` が要る（`check.ps1` が作るもの）ので、手で作ってから再 configure した。成果物は `build/issue91/NeNeNib.exe`。CMake の出力先は既定へ復元していない（worktree ごと捨てる前提）。

## Issue #98: fixtures.jsonの整形を1つに固定

統合単位は [PR #105](https://github.com/hideyukiMORI/nene-nib/pull/105)。**draftのまま**（Ready・必須check・merge・CLAUDE.md 6 節の更新は設計リナが行う）。ブランチ `chore/98-fixtures-json-format` は main `b07c574` の上に4 commit（生成器 → 1回きりの整形 → CNF-011 と反例 → 文書）。作業ツリーはclean。

**いまの状態**: `tests/vim/fixtures.json` は977件すべてが1行1件の正準形で、バイト列は96756 bytes・979行・SHA-256 `d86f6d3214c9bd8f62f31426a219c1faa834df0ec633a18b8792027a681619a1`。`tests/vim/VimFixtures.hpp` は同じSHAを名乗る（変わったのはこの1行だけ）。整形の正本は `eng/vim-oracle.py` の `canonical_fixtures_json` 1か所で、`--format`（Vim不要・生成行は `--reuse-ref` から逐語再利用）と `--regenerate`（測定前に書き戻す）が書き、CNF-011（`eng/conformance.py` の `fixture_format_checks`）が検査する。

**次に fixture を足す人への注意が2つある。**(1) 追記は1行1件で書けばよい（既存の668行と同じ形がそのまま正準形）。手で整えなくても `--regenerate` / `--format` が書き戻し、CNF-011 が違いを行番号つきで言う。(2) 空の `"settings": []` は**書かない**のが正準なので、#98 より前の commit を `--reuse-ref` にした `--only` の部分再生成は「非選択の入力が変わった」として拒否される（1回きりの移行。#98 以降の ref なら通る。整形後に固定Vim 9.1で `--only search-dot-count` を回して 1 measured / 976 reused・生成物バイト同一を確認済み）。

**検証**: `--format`（0 measured / 977 reused・2回目もべき等）、`--regenerate --only search-dot-count`（1 measured / 976 reused・差分なし）、`eng/conformance.py` 0 violations と実リポジトリの反例2通りで終了1、`test_conformance.py` 74 tests（69 → 74）、`test_vim_oracle.py` 16 tests（10 → 16）、`nib_tests` 引数なし10149 checks（#100と同数）、`ctest -R nib_unit` 2.13 s、生成物の `clang-format --dry-run --Werror` 指摘なし。production の C++・入力・期待値・閾値が不変なので symbols・カバレッジ・速さ・Release・全件は実行していない（QLT-001 / QLT-012・ADR 0021）。詳細は [gate-proofs 5-aa](../quality/gate-proofs.md)。ログは `out/issue98-*.log`（`out/` は追跡外なので作業機にだけある）。

**設計リナへの申し送り2件**: (1) `eng/test-conformance.py`（conformance全体）は `test_verification_policy.py` の6件で落ちるが、**この機械の端末符号化（cp932）による既存の失敗**で #98 が原因ではない（未変更の `b07c574` でも同じ5件が落ち、6件目の GIT-003 は main の commit `8f2c363` を同じ経路に流しても落ちる。`eng/validate-git.ps1` が `git show` のUTF-8出力を pwsh の入力符号化で受けているのが原因で、Issue #94 が直したのはpython側の出力だけ）。別Issueに切るのが妥当。(2) gate-proofs の節番号は #91 の並行作業を避けて 5-aa を取った（5-x は空けたまま）。

## Issue #100: Vimの検索（`/ ? n N * #`）

統合単位は [PR #103](https://github.com/hideyukiMORI/nene-nib/pull/103)。**draftのまま**（Ready・merge・CLAUDE.md 6 節の更新は設計リナが行う）。ブランチ `feat/100-vim-search` は origin/main `eb79415` の上に ADR 0032 の 3 commit ＋ 実装 4 commit。

設計側（Fable）の依頼。ADR 0032 は実測と実装のあとで**受理**にし、食い違った 1 点（`\<あいう\>` はひらがな → 漢字の境界で `あいう漢字` に一致する）は期待値ではなく補足を直した。ほかの決定 1〜7 はそのまま通った。

入力行は `CommandInput` の 3 つめの値 `SearchLine`、見え方は `InputLineView`（プロンプトの閉じた enum ＋ 文字列 ＋ caret ＋ 候補）1 つに畳んで描画を 1 本にした。編集の純関数は `InputText` で Ex と共用。照合器 `VimPattern`（`magic` の部分集合・未対応構文は `VimPatternFailure` で拒否）と走査 `vim_search`（行ごとに数え直し・折り返しを返す・回数は呼ぶ側が繰り返す）は core の純関数で、`std::regex` と `<locale>` は使っていない。engine は `VimKey` に `VimSearchPattern`、`VimEffect` に `VimOpenSearch`、`VimState` に `last_search`、`VimStep` に `notice`。exclusive の規則は `exclusive_range` 1 か所を移動と検索が共用し、`*` / `#` は語の先頭から探して範囲の端は元のキャレット。報せの文言は `vim_search_message` 1 か所。

**要判断は無し。**実装側で選んだのは 3 点。(1) `VimAction` が 48 を超えて NORMAL / VISUAL の分岐が関数長 60 行（T8）に収まらなくなったので、CPP-012 のとおり「動作 → 大分類」を `constexpr` の表にし、`VimActionGroup` を網羅する switch 2 つで写した（閾値は触っていない）。表の行の欠落と重複は `static_assert` 2 つが落とす（動作の個数 `vim_action_count` を末尾の値から導いて表の大きさに固定し、0 以上 個数未満のどの値も表にちょうど 1 行あることを `constexpr` の関数で確かめる）。動作を 1 つ足して行を足さない形が実際にコンパイルで落ちることを確認した（設計リナのレビューで受理・この形に直した）。(2) 折り返しの報せは効果と同時に出るので `VimEffect` ではなく `VimStep.notice` で運ぶ。(3) 入力行の取消が捨てるもの（保留オペレータ・回数・組み立て中の鍵）は engine の純関数 `vim_cancelled_input` が決め、controller はそれを呼ぶだけ（#92 の二重経路を広げないため）。

fixture は 135 件を採用して 842 → 977 件。候補 136 件のうち 1 件（空入力の Backspace の取消）は「区切った形」と「1 回の `:normal!` の形」で答えが違うので `add-fixtures.py` が機械的に拒否した。`--only search-` の部分再生成で 135 件だけを測り、既存 842 行は逐語再利用（削除行は metadata 2 行）。977 件の入力 SHA-256 は `2e36c3b63bd65ea79ce3fed9af4c1423d70f8d6376005570bf3119c10b546ac1`（来歴の 842 件は `58ba91c9…`）。2 回目の再生成はバイト単位で同一。初回の再生で 135 件すべてが実装と一致した。

`--vim-search` 1375 checks、`--vim-dot` / `--vim-text-objects` / `--vim-character-search`（909 / 1623 / 621）、`--ex-settings` / `--command-palette`（153 / 130）、`--coverage-negative` 22、unit 全体 10149 checks、`ctest -R nib_unit` 2.33 s、build（Debug / tidy / ASan / UBSan）、symbols 2libs/0、conformance 0、format がすべて成功。詳細は [gate-proofs 5-z](../quality/gate-proofs.md)。同じ入力で測り直さない。

**後続**: `:s` `:g`・検索履歴・offset・`\v` `\c` `\(` `\|` `\{`・`ignorecase` / `smartcase`・`hlsearch` / `incsearch` の描画。後ろ向きの検索で `?` そのものを探すことは部分集合の外（`\?` は量指定子として拒否する）。`fixtures.json` の整形は #98 を次に 1 回。性能は測っていないので、**照合が本文の行数と長さに比例する**ことを次に速さを測る機会に ADR 0016 の基準値と突き合わせる（残るリスクはこれだけ）。実機の画面確認は未実施（native pipe が繋がらない）。成果物は `build/issue100/NeNeNib.exe`（SHA-256 `93c3029038b12855f00e24bc78e8fdc1e013f6de8f22ac7102af896e5d70d561`）で、`build/NeNeNib.exe` は旧版。

## Issue #93: Vimのテキストオブジェクト

統合単位は [PR #96](https://github.com/hideyukiMORI/nene-nib/pull/96)。**draftのまま置いてある**（依頼どおりReadyにもmergeもしていない）。draftのあいだ `check.yml` は `draft == false` の条件でskippedになるので、必須checkはReadyにしてから回る。

設計側（Fable）の依頼。ADR 0031 の1 commitを積んだ `feat/93-vim-text-objects`（origin/main `d26c232` の上）で実装し、ADR 0031は実測のあとで受理した。統合状態はGitHubが正。

engineは `VimInputWait` に3つめの値 `VimTextObjectScope`（inner / around）を持ち、オペレータ保留中とVISUALの `i` / `a` だけがこの待ちに入る（NORMALの `i` / `a` は挿入のまま）。次の鍵は15行の表で閉じた `VimTextObject` へ写り、`b` / `B` と閉じ括弧の鍵 `)` `}` `]` `>` も同じ行。範囲は新しい純関数 `vim_text_object_range(text, selection, request, count)` 1本が決め、オペレータの後ろは既存の `performed`、VISUALは選択の置き換えへ同じ範囲を渡す。`selection` を引数に取るのは、VISUALで選択があるときの「もう1つぶん広げる」（`viwiw` / `vi(i(`）を同じ関数の中に閉じるため。語の種類の表は `vim_character_class` として `VimWordMotion` から公開し、`iW` は同じ表の畳み方の違いだけにした（`VimScanPoint` が語の切れ方を持つ）。

**判断が要る点は無い**が、ADR 0031 の決定を実測で4点直した（VISUALは行単位にならず改行まで届く・括弧は中に居なくても前の塊を使い向きは最初の1回で決まる・`i(` の2つの寄せは独立で両方のときだけ行単位・`y` のキャレットは範囲の先頭の桁）。実測と合わせていないのは、回数が尽きた `d9iw` でVimがキャレットを本文の終わりへ動かすことと、塊の外から数えた `2i(` がVimでは1段ずつ内側へ入ることの2点。どちらもfixtureに採らず、ADR 0031の「文脈」に書いた。

実測は実装の前に364ケース（`out/issue93-oracle/probe*.py`）。採用fixtureは192件で842件になり、既存650行は逐語再利用。842件の入力SHA-256は `58ba91c91e65162e9c651acb53711f9b9c5d68e96bc70c1231874c9d1c0ee94a`。`--vim-text-objects` 1623 checks、共有境界（`.`・r・f/t・g・VISUAL yank）とunit全体8733 checks、build/tidy/ASan/UBSan、symbols 2libs/0、conformance 0、formatがすべて成功。詳細は [gate-proofs 5-w](../quality/gate-proofs.md)。同じ入力で測り直さない。

**後続**: `it at`（タグ）・`is as`（文）・`ip ap`（段落）、`Ctrl-v`、`> < gu gU` のオペレータ、VISUALの後ろ向きの選択を伸ばす規則。既存の穴は #85（literal CR）・#91（VISUALの `.`）・#92（入力記録の二重経路）。

## Issue #87: `.` による直前の変更の再生

統合単位は [PR #90](https://github.com/hideyukiMORI/nene-nib/pull/90)。必須checkはGit規約・検証記録・空白を確認し、成功した対象検証は再利用する。**Readyにした。merge は hide / 設計リナが行う。** draft のあいだ `check.yml` は `draft == false` の条件で skipped になる。Ready 直後の1回は PR 本文の `確認する退行:` が見出しだけで中身が同じ行に無く GIT-004 で落ちたので、テンプレートどおり同じ行に書き直して通した（閾値・検査は変更していない）。

設計側（Fable）の依頼。main `82f260d`（#84 / PR #86）から `feat/87-vim-dot-repeat` で実装。ADR 0030は実測のあとで受理へ更新した。統合状態はGitHubが正。

engineは `VimState` に `recording` / `last_change`（`optional<VimRepeatRecord>` ＝ 回数1つと回数の桁を除いた `VimKey` の列）を持ち、`vim_step` の後段にある純関数1か所だけが記録を書く。分けるのは鍵の意味ではなく「前のモード」と「効果」で、変更を起こす6効果が確定、移動・yank・undo/redo・`:` は破棄、INSERTはEscで1つの変更として確定し、Home/End/矢印/ページ移動は記録を `i` から取り直す。`.` は `VimAction::repeat_change` → `VimReplay`（回数の桁を先頭に展開した鍵の列）で、controllerは前後で履歴を閉じて同じ `accept(VimKeyPress)` へ1鍵ずつ流す。`.` 自身は記録されないので再帰は深さ1。UI・IME・描画・保存形式は変更していない。

**判断点1は取り下げた（測り方の誤り）**。当初「Vimは失敗したオペレータで記録を入れ替える」と読んだのは、失敗してビープする命令のあと `:normal!` の残りの鍵が捨てられるためで（Vimソースは読んでいない）、`xdkj.` では `j` も `.` も実行されていなかった（最終カーソルが1行目のままなのが証拠）。鍵を命令の切れ目で区切って測り直すと（`probe3.py` 28件・`probe4.py` 21件×2形）、取消になった命令は自分の鍵を捨てるだけで直前の変更を変えず、**ADR 0030 決定3のままで固定Vimと一致する**。engineは変更していない。決定3の本文に実測した12経路（範囲の作れない `dk`/`dj`/`2dd`/`2D`、回数の入らない `3r`、外れた `f`/`;`、motionでない `dq`、`g` の続きが無い `dgz`、失敗した `yk`、Escの取消 `d<Esc>`/`r<Esc>`）を書き足し、`--vim-dot` の対象unitで守る。

ビープする命令の後ろに鍵が続く列は、1回 `:normal!` の既存生成器では fixture にできない（生成器が残りの鍵を捨てた答えを記録する）。生成器は変更せず、ビープしないEscの取消 `xd<Esc>j.` と `xr<Esc>j.` の2件だけをfixtureに採った。

**判断点3（ADR 0028の拡張）は受理された。** `N.` が挿入命令を繰り返すには `3ifoo<Esc>` が `foofoofoo` である必要があり、回数付き `i a I A` が回数を捨てていた穴を `VimInsertRepeat` の既存機構で塞いだ。ADR 0028 の末尾に補足を足して置換関係を明示した。あわせてcontrollerの `interrupt_vim_insert`（外からの割り込み）と `perform(VimMoveTo)`（Vimの鍵による移動）を分けた。

**後続Issue**: VISUALで行った変更の `.` は [#91](https://github.com/hideyukiMORI/nene-nib/issues/91)。controllerが `VimState` の入力記録を直接消す二重経路は [#92](https://github.com/hideyukiMORI/nene-nib/issues/92)（今回は広げない）。

実測は実装の前に固定Vim 9.1を201ケース起動（`out/issue87-oracle/probe.py` 172 ＋ `probe2.py` 29）。決定2・4・7は一致。証拠は `out/issue87-oracle/probe*.json` / `probe*.txt`。Vimソースは読んでいない。

採用fixture99件で551→650。部分再生成で99件だけ測り、既存551行は逐語再利用（削除行はmetadata 2行のみ・既存97行は再測定でも同じ値）。650件の入力SHA-256は `e7d90990f8eed17ae5b3ce86b8da6823a074f492eb65b5753b00a10276a8b4fb`。`--vim-dot` 909 checks、共有境界の `--vim-open-lines` / `--vim-open-line-external` / `--vim-open-line-recovery`（565 / 46 / 430）と `--vim-replace` / `--vim-character-search` / `--vim-line-jumps`（460 / 621 / 989）、unit全体7181 checksがすべて成功。Debug/tidy/ASan/UBSan build、symbols 2libs/0、conformance 0、formatも成功。初回buildの関数長拒否は共通 `scroll_action` への整理で修正し、閾値は変えていない。詳細は [gate-proofs 5-v](../quality/gate-proofs.md)。同じ入力で測り直さない。

性能は測っていない。1打鍵あたり小さな `vector` の複製が増えるので、次に速さを測る機会にADR 0016の基準値と突き合わせたい。computer-useによる実機の画面確認は未実施で、起動中の旧版PIDには触れていない。成果物は `build/issue87/NeNeNib.exe`、SHA-256 `19952fcabafd0f1885c2a732802ec89b1ea238f651722e401e0e8371769da068`。`build/NeNeNib.exe` は旧版なので取り違えない。CMakeの一時出力先は既定へ復元済み。

保存schema・依存・ゲート変更なし、Waivers: none。次は #91（VISUALの `.`）と #92（入力記録の二重経路）、Issue #85のliteral CR、残る `Ctrl-v`・テキストオブジェクト・一般Exを焦点Issueごとに進める。
