# 日報 — 2026-09-23 / NeNe Nib

## 設計リナの総括（2026-09-23・施主指示の反映と区切り）

短いセッション。統合は文書 1 本（[PR #132](https://github.com/hideyukiMORI/nene-nib/pull/132)・ADR 0038）で、main は `f17dc9e` → `bfa91f0`。production・fixture・ゲートは変えていない。

**施主指示（hide・2026-09-23）**: Opus 5 の消費が速い。原因は判断を含まない仕事まで Opus 5 に出していること。次のセッションから背景席を立てるときは仕事の種類でモデルを明示し（実装＝Opus 5・下ごしらえ＝Sonnet 5・機械作業＝Haiku 4.5・裁定と受理は設計席自身）、同じ手順を 2 回以上モデルに踏ませたら 3 回目は `eng/` のスクリプトにする。指示の型は複数リポジトリ共通なので、Nib に無い語（`tools/`・golden / replay・mailbox・launcher）は [ADR 0038](../adr/0038-model-per-seat-and-scripted-preparation.md) の読み替え表で写した。「保護対象」は fixture の期待値・生成物・`perf-reference.json`・allowlist・conformance の規則・保存 schema・ゲートの閾値、と設計席が決めた（hide 未確認）。強制は planned（Agent の呼び出しはリポジトリの外）。

**施主指示 手 3（同日・夜）**: 9/22 の transcript の集計で Opus 5 の消費は全部が実装席で、1 席 150〜400 ターン・文脈 350〜800K・cache read 0.4〜1.7 億トークン。「ターン数 × 文脈長」の積み上げなので席の長さが二乗で効く。実装席を工程（probe / 実装 / 差し戻し対応）ごとに新しい席にし、道具出力を小さく、task を S 級に、最終報告を 30 行以内にする（[ADR 0039](../adr/0039-implementation-seat-per-step-and-small-tool-output.md)・#136・依頼書の型 `docs/templates/implementation-seat-brief.md`）。次に立てる Opus の実装席から。

**起票**: #128（本指示の記録・統合済み）、#129（実機用 Release を ref から作り SHA を記録する・起動しない）、#130（保護対象の差分 0 確認と fixture の逐語再利用・scope ごとの checks 数の前後比較）、#131（verify-window の PNG 保存と前後の画素比較）、#134（本日報）。

**#129 の状態**: 実装席（Opus・`model` 明示の最初の例）で開始。利用上限（429）で 1 度止まり、上限が戻ったあと同じ席に「続きを」で再開した。区切りの時点でスクリプトの commit と draft [PR #133](https://github.com/hideyukiMORI/nene-nib/pull/133) まで進んでいる。受理（Ready・CI・merge）は次のセッションの設計席。

**教訓**: (1) 429 は実装席の起動直後にも起きる。再開は同じ agent へ 1 行で足りる（commit 済みの所は残る）。(2) 実装席が本体ツリーで作業中の docs は worktree で書く（本日の日報がそれ）。(3) 依頼書は Issue 本文を正本にして短くできる。小さい chore は依頼書を別に書かず、Issue と ADR を指して席とモデルを 1 行で伝える。

実機の画面確認は今日は無し（前夜に hide が #123 まで確認済み・「全体的にいい」）。速さは測っていない（差分が文書のみ）。

## Issue #129: 実機用 Release の作成と SHA の記録をスクリプトに（実装リナ・Opus 5）

ADR 0038 の決定 5（同じ手順を 2 回モデルに踏ませたら 3 回目はスクリプト）の 1 本目。`eng/build-release.ps1 -Ref <ref>` が `eng/toolchain.ps1` で `CXX=clang-cl` を固定し、`build/release-<短い SHA>` に Ninja の Release を作って SHA-256・ref・commit・HEAD・所要時間・道具の版を `out/release/<短い SHA>.json` に記録する。**起動はしない**（起動は設計席）。
ref を名指ししない呼び方・無い ref・dirty な作業ツリーは、toolchain に触れる前に終了 1（QLT-013）。ref が HEAD と違うときだけ `build/` の下に worktree を一時的に作って build し、終わったら消すので本体の作業ツリーは動かない。既定の `build/`・`build-release/`・起動中の exe には触れない。
検証は `-Ref main`（worktree 経路・96.0 s）と `-Ref HEAD`（本体ツリー経路・104.5 s）の 2 本が終了 0、反例 3 通りが終了 1（sentinel に届かない）、`eng/test-conformance.py` 163 tests OK（160 → 163）、`eng/conformance.py` 0 violations。C++ に触れないので build / unit / symbols / 速さ / 全件は未実行。
**受け入れ条件の実測**: 2026-09-22 に手で作った `build/release-main-123/NeNeNib.exe` と、スクリプトの生成物は 978944 バイトのうち**違うのが 2 バイトだけ**だった（offset `0x80` の PE `TimeDateStamp`）。つまり「手で踏んだ手順」と「スクリプト」は同じものを作る。`/Brepro` は入れていないので、同じ commit でも作るたびに SHA はこの 2 バイトぶん変わる（SHA は「その commit の印」ではなく「この 1 回の生成物の印」として読む）。
統合単位は draft [PR #133](https://github.com/hideyukiMORI/nene-nib/pull/133)、詳細は [gate-proofs 5-ak](../quality/gate-proofs.md)。Ready・CI・merge は設計席。

## 設計リナの総括（2 回目・2026-09-23・手 3 の適用初日）

main は `f677a7e` → `0cab39c`。統合は 4 本: #124（README・current.md の要約化・日報の粒度の約束・PR #139）、#131（verify-window の PNG 保存・領域ごとの画素比較・無変化は失敗・PR #138）、#141（依頼書の型の報告ファイル名・PR #142）、#130（保護対象の差分 0 確認のスクリプト・PR #143）。production・fixture・ゲート・schema は変えていない。

**手 3 の初日の実測（席の本数・工程・トークン）**: 立てた席は Sonnet 3 席（probe #130 / #131・棚卸し #124）と Opus 5 席（#131 実装・差し戻し 2 回、#130 実装・差し戻し 1 回）。1 席 = 1 工程で、`SendMessage` の使い回しと `fork` は 0。各席のトークン（Agent の完了通知の `subagent_tokens`）: probe #131 92K・probe #130 87K・棚卸し #124 117K・#131 実装 131K・差し戻し 1 84K・差し戻し 2 82K・#130 実装 111K・差し戻し 1 80K。**最大でも 1 席 131K**。9/22 の「1 席 cache read 0.4〜1.7 億」とは物差しが違う（完了通知の値と transcript の累計）ので、同じ物差しでの比較は usage の集計スクリプト（未起票）を待つ。最終報告はどの席も 30 行以内だった。

**教訓**: (1) 受け入れ条件は実装席の実測で 2 回直った（#131「本文の内側」→「期待した領域の外に差分が無い」、#130「#99 は終了 0」→「`--allow` で名指しすれば 0」）。訂正は Issue のコメントに残し、依頼書はそれを指す。(2) 「無変化を成功に見せない」を 2 回適用した（#131 の `changed: false` → 終了 1、#130 の scope が読めなければ終了 2）。(3) Write ツールが `report-*.md` を拒む席があった（#141）。(4) CI は draft のまま作った PR には走らない（`ready_for_review` / `synchronize` / `edited`）。draft → Ready の順で作るか、本文を編集して走らせる。(5) GIT-004 の欄名は `検証結果:` の形で同じ行に値が要る（実装席の PR 本文で 2 回落ちた。依頼書の型に書いた）。(6) 設計席が PNG を Read で見て受理できた（#131・800×450）。目視の代わりになる。

## Issue #124: README・current.md・日報の粒度（設計席・棚卸しは Sonnet）

README の機能段落を「What works today」（FR-001〜017・済 12 / 一部 3 / 未 2）に置き換え、`current.md` を 134 行 → 54 行にし、DEVELOPMENT_WORKFLOW 8 節に日報の粒度の約束を 1 段落。棚卸し（probe-124.md）は #85 / #123 を未 merge と読み違えていたので設計席が正した（fixture 1339 件・13309 checks は main の値）。PR #139。検証は conformance と相対リンクだけ。

## Issue #131: verify-window の PNG 保存と領域ごとの画素比較（実装席 Opus・3 席）

`window_driver.py` に `capture` / `write_png` / `read_png`（zlib だけ）と鍵の記法、`verify-window.py --capture <dir>` と `--keys`、`compare-frames.py`（`--inside` / `--regions` / `--expect`）。実測 `ihello<Esc>` は本文・タブの「●」・ステータスの「行 1, 桁 7」の 3 領域に差分があり外側は 0。鍵が届かない回（4 回中 2 回）は `changed: false` で終了 1（原因は #140）。conformance 163 → 177。PR #138・[gate-proofs 5-al](../quality/gate-proofs.md)。

## Issue #130: 保護対象の差分 0 確認のスクリプト（実装席 Opus・2 席）

`eng/protected-diff.py --base <ref> [--head] [--allow <scope>] [--build]` が fixture の差分（metadata 2 行と削除・変更・追加）、保護対象 5 ファイルの差分の有無、scope ごとの checks 数（`scopes` 配列の静的パース・`--build` で base と head の `nib_tests` を作る）を `out/protected/<sha>.json` に書く。実測 3 例: #125 終了 0（`--vim-search-highlight` 新規 74）、#85 終了 1（metadata 2 + 削除 10・行番号つき・12 scope の checks が変化）、#99 は `--allow --vim-text-objects` で終了 0。scope が読めなければ終了 2。conformance 177 → 185。PR #143・[gate-proofs 5-am](../quality/gate-proofs.md)。

## Issue #141: 依頼書の型の報告ファイル名（設計席）

`report-<issue>-<工程>.md` を `probe-<issue>.md` / `done-<issue>-<工程>.md` に。ADR 0039 の読み替え表と引き継ぎも。PR #142。

## 設計リナの総括（3 回目・2026-09-23・「どんどん進めて」の続き）

hide の「いいよ。どんどん対応を進めて」を受けて、main は `d4de8c2` → `dc3ebf6`。統合は 5 本: #146（usage の集計スクリプト・PR #149）、#117（制御文字の `^M` 描画・ADR 0040・PR #150）、#151（ブロックキャレットの幅・PR #153）、#147（C1 制御文字の 4 桁・PR #155）、#152（IME の変換中の行の色・PR #156）。#148（incsearch・ADR 0041・PR #154）は実装 2 席が終わり設計席が main に rebase 済みで、差し戻し 1 本（`hlsearch` off の入力中は今の当たりだけ・rebase 後の検証の撮り直し）を残して draft。

**計測して言う（`eng/usage-report.py --since 2026-09-23` の実出力・gate-proofs 5-an の続き）**: 席は Sonnet 7・Opus 5.5 が 14・Opus 5 が 1（前の session の #129）。1 席の最大は 62 ターン・文脈 157K・cache_read 671 万（#148 の application の席）。9/22 の 1 席（217 ターン・430K・6,132 万）と比べて cache_read は 1 席あたり 9〜50 分の 1。設計席（Fable・この session）は 138 ターン・最大文脈 361K・cache_read 2,902 万で、背景席 14 本の合計（3,744 万）に近い。**今日の `model: opus` は `claude-opus-5-5` に解決されていた**（手 3 の 3-5 の試行は今日が初日。枠の減りは hide の側で見る）。

**教訓**: (1) 受け入れ条件の訂正は今日も 2 回（#130 `--allow`・#131 領域）に加えて #148 で 1 回（`hlsearch` off）。実装席の「設計席に確認してほしい点」は毎回 1〜3 件あり、どれも設計の穴だった。(2) 同じ日に複数の枝が gate-proofs の末尾に節を足すと rebase で毎回衝突する（5-an〜5-aq で 3 回）。節番号は PR の順で設計席が改番する。(3) rebase 中に `;` で繋いだ掃除コマンドが先に走って worktree を消した（#147）。origin の枝が無傷だったので作り直せたが、rebase と掃除は別のコマンドにする。(4) 設計席が PNG を Read で見る受理が 4 回（#117 `a^Mb`・#151 の zoom・#148 `/be`・#131）でどれも一目で判断できた。(5) clang-tidy の引数 4 つの上限に 2 席が当たり、どちらもゲートを弱めず型に束ねた（`VimMatchRequest`・`draw_clauses` の `base`）。

## Issue #146: transcript の usage を席ごとに集計するスクリプト（実装席 Opus 5.5・1 席）

`eng/usage-report.py [--since] [--until] [--json]` が jsonl 1 本 = 1 席で turns・最大文脈・cache_read・output・seat_tokens（完了通知の `subagent_tokens` に相当）を表と JSON に出す。同じ `message.id` の複数行は 1 ターン（output は最終値）。本文は読まない。conformance 185 → 191。PR #149・[gate-proofs 5-an](../quality/gate-proofs.md)。

## Issue #117: 制御文字を `^X`・書式用文字を `<xxxx>` で描く（ADR 0040・実装席 Opus 5.5・2 席）

core の純関数 `display_line` が置き換え済みの文字列と桁の対応表 `starts` を作り（scope `--display-line` 123 checks）、`LineView.display` を renderer が layout に使って桁は対応表で写し、置き換えた文字は `muted` で `tint_runs`（application の unit +27）。PNG で `a^Mb` / `^Ax<200b>y beta` を確認。`verify-window.py --open <file>` を足した。残るリスク 2 点は #151 / #152 に起票。PR #150・[gate-proofs 5-ao](../quality/gate-proofs.md)。

## Issue #151 / #147 / #152: #117 の残差（実装席 Opus 5.5・各 1 席）

#151: ブロックキャレットの幅を `display_position` の両端で決め `^M` の 2 文字を覆う（PNG の zoom で確認・`verify-window.py --vim` を足した・PR #153）。#147: `DisplayWidth::hex`（4 桁）を足し C1 制御文字を `<85>` の形で描く（Vim の `strdisplaywidth("\x85") == 4` を設計席が実測・switch 3 本を `default` 無しで埋めた・checks +46・PR #155）。#152: #152（IME の変換中の行の色・PR #156）。

## Issue #148: incsearch（ADR 0041・実装席 Opus 5.5・2 席・draft）

core: 検索の次の一致を `vim_find_match(text, pattern, VimMatchRequest)` 1 本にし `vim_step` と preview が同じ関数を呼ぶ（fixture 1339 件不変・`--vim-search` 1290 → 1356）。application: `EditorState` の `SearchPreview`（`match` と `origin` の scroll）を 1 文字ごとに更新し、Esc で `origin` へ戻し、Enter は今までどおり engine の 1 つの鍵（scope `--vim-search-incremental` 108 checks・PNG で `/be` の当たりを確認）。`:set (no)incsearch`・既定オン（hide 未確認・D18 候補）。PR #154 は差し戻し 1 本を残して draft。

## 設計リナの総括（4 回目・2026-09-23・「日報と引き継ぎ書を読んで対応を進めて」）

hide の指示は「日報と引き継ぎ書を読んで対応を進めて」。引き継ぎ 09-23「3 回目の区切り時点」の次の順どおりに、main は `de0eebd` → `bfac0b3`。統合は 7 本: #148 incsearch（差し戻し 1 席・PR #154）、#162 docs（PR #161）、#160 単体テストの分割（ADR 0042・PR #163）、#165 conformance の既存失敗（PR #166）、#140 `--keys` の揺れ（診断 1 席＋直し 1 席＋差し戻し 1 席・PR #167）、#168 incsearch の Ctrl-G / Ctrl-T（ADR 0043・PR #169）、本節の docs。open な Issue は 0。

**計測して言う（`eng/usage-report.py --since 2026-09-23` の実出力・4 回目のぶん）**: 席は Sonnet 3・Opus 5.5 が 8。1 席の最大は 76 ターン・文脈 218K・cache_read 1,126 万（#160 の分割席・37 ファイルへの移動と照合）。次が #168 の実装席（66 ターン・166K・759 万）。背景席 11 本の cache_read の合計は Opus 3,049 万・Sonnet 781 万。設計席（Fable・この session）は 118 ターン・最大文脈 280K・cache_read 2,034 万で、背景席の合計（3,830 万）の半分強。`model: opus` は今日も `claude-opus-5-5` に解決。

**教訓**: (1) 受け入れ条件の訂正は今日も続き、#148（`hlsearch` off）・#160（scope 21・翻訳単位 27・7 % 遅い）・#168（Ctrl-T の起点・回数・向き）で ADR の本文を実測に合わせて設計席が直した。実装席の「設計席に確認してほしい点」は毎回 1〜3 件で、どれも設計の穴だった（運用の約束 19 のとおり差し戻しは 1 命題 = 新しい席）。(2) rebase の自動 merge が `NibTests.cpp` の閉じ括弧を 1 つ落とした（#148）。衝突が無くても rebase 後は組んで確かめる。(3) docs だけの PR にも `Closes #N` と GIT-004 の欄が要る（#161 で 1 回落ちて Issue #162 を切った）。(4) 枝名は `feat|fix|docs|refactor|test|build|ci|chore` だけ（#140 で `eng/` を付けて GIT-002 に落ち、改名の前に PR を閉じたので head を失って #167 を開き直した。改名 → 開き直しの順）。(5) headless の Vim（`-es` ＋ `:normal!`）は incsearch を対話と同じには動かさない（`/be<Esc>` が元に戻らない）。incsearch 系は fixture にせず契約で守る（ADR 0041 / 0043）。(6) 同じ機械で 2 つの席が verify-window を走らせると `FindWindowW` が先の窓を返して 2 本目は `WindowUnavailable` になる（重なりの反例は補助スクリプトで作った）。前面を別の窓（Chrome）が持つと first paint と IME の節が落ちる。撮影は 1 席ずつ、前面は空けておく。

## Issue #148: incsearch の差し戻しと merge（実装席 Opus 5.5・1 席）

設計席が main に rebase（gate-proofs 5-aq → 5-as）。差し戻し席が `hlsearch` off の入力中は `matches` を出さず preview の枠だけにする契約 2 つと rebase 後の検証（既定 13682・`--vim-search-incremental` 111・protected-diff fixtures 1339 不変）を撮り直し、rebase で落ちた閉じ括弧も直した。PNG（`/be` で `beta` の be に面と枠）で受理。既定オンは hide 未確認（D18 候補）。

## Issue #160: 単体テストを scope ごとの翻訳単位に分ける（ADR 0042・実装席 Opus 5.5・1 席・棚卸しは Sonnet）

Sonnet の棚卸し（今のゲートは 1 つも落ちていない・`VimStep.cpp` を割るには内部ヘッダが要り CPP-008 と衝突）→ ADR 0042: `NibTests.cpp`（8224 行）を 1 scope = 1 翻訳単位に分け、表 2 つと `main` は `NibTests.cpp`（137 行）に残す。`VimStep.cpp` はテーマで割らず純関数の切り出しだけ。実装席は 1 度きりのスクリプトで 37 ファイルに分け、実体 383 個のうち 382 個が一字一句同じことを照合。既定 13682 不変・scopes 21 / same 21・クリーンビルドは 87.8 s → 94.0 s（7 % 遅い）。ADR の本数は設計席が実測に合わせて補正。

## Issue #140: `--keys` の鍵が届かない揺れ（Sonnet の probe → Opus の診断・直し・差し戻し各 1 席）

probe（読むだけ）: PostMessage の経路・フォーカス無関係・IME 無関係・`renderer_ == nullptr` の門は通常経路で発火しない。診断席: `drive_keys` に `--measure` を通し `input_received` / `frame_presented` の節目を `frames.json` に写す。16 回とも鍵 8・frame 3・`changed: true` で再現せず。5-al の失敗回の `after.png` は正常な `before.png` と同じ大きさ →「別の窓を撮っていた」が最有力。直し席: `assert_uncovered`（中心と四隅の内側の 5 点で `WindowFromPoint` → `GA_ROOT` が自分か）を `capture` の先頭に置き、覆われていれば `covered: true` と相手のクラス名・pid を書いて終了 1。直列 8 回・反例 1 回。差し戻し席: 既存の `covered_by`（中心 1 点・measure-speed と first paint）も同じ規則に 1 本化。原因は確定していないが、次に揺れた回は `coveredBy` と `measure` で切り分けられる。

## Issue #165: test_protected_diff が scope 数を 19 と固定（実装席 Opus 5.5・1 席）

#140 の席が見つけた main の既存失敗（`eng/test-conformance.py` 1 件）。`NibTests.cpp` の `std::array<..., N> scopes` の `N` を読んで一致と重複無しを確かめる形に。

## Issue #168: incsearch の Ctrl-G / Ctrl-T（ADR 0043・実装席 Opus 5.5・実装 1 席＋差し戻し 1 席・probe は Sonnet）

Sonnet の probe が本物の Vim 9.1 を oracle と同じ呼び方で叩き、Enter で終わる列は決定的だが Esc は元に戻らない（headless の穴）と報告 → fixture にせず契約で守る。ADR 0043: preview が検索の起点 `from` を持ち、Ctrl-G / Ctrl-T は起点を当たりへ動かして次／前を探す。確定の鍵 `VimSearchPattern` が `optional<TextPosition> from` を運び、engine は `from.value_or(caret)` から `vim_find_match` 1 本。実装席の確認点 3 つ（Ctrl-T の後に Enter が着く起点は「確定の検索が着く位置」・preview も確定と同じ回数・向きは本文の順）はすべて設計の穴で、ADR の決定 2 を設計席が直して差し戻し 1 席。差し戻し後は既定 13712・`--vim-search-incremental` 141・fixture 1339 不変。PNG（`/be` → Ctrl+G で枠が 2 つ目の be へ・キャレット不変）で受理。

## 設計リナの総括（5 回目・2026-09-23・「進めて」）

hide の「進めて」を受けて、引き継ぎ「4 回目の区切り時点」の次の順（add バッファ → マクロ → Tab の画素幅）を Sonnet の probe 3 本から始め、main は `86ab4ba` → `8387d9f`。統合は 6 本: #175 Tab の tab stop（ADR 0045・PR #177）、#174 add バッファの chunk 化（ADR 0044・PR #178）、#176 マクロ `q` `@`（ADR 0046・PR #181）、#180 `recording @a` の表示（PR #183）、#179 16 MiB を開いてから 200 打鍵するベンチ（PR #182・実機の基準値 427.7 ms）、#184 改行の索引の共有（ADR 0047・PR #185・16 MiB の 200 打鍵 462 → 7〜9 ms）。open な Issue は 0。

**計測して言う（`eng/usage-report.py --since 2026-09-23` の実出力・5 回目のぶん）**: 席は Sonnet 4・Opus 5.5 が 7。1 席の最大は #176 マクロの実装席で 116 ターン・文脈 337K・cache_read 2,728 万（core・application・oracle・fixture 20 件・契約を 1 席で・S 級 3 命題の上限）。次が #184（43 ターン・139K・444 万・429 で 1 度止まり同じ席で再開）。背景席 11 本の cache_read の合計は Opus 4,785 万・Sonnet 1,007 万。設計席（Fable）は 5 回目のぶんで 105 ターン・最大文脈 520K・cache_read 4,330 万で、背景席の合計（5,792 万）の 4 分の 3。設計席が計測（`measure-speed` を 12 回）と rebase の衝突（gate-proofs 4 回・README 2 回）を自分で行ったぶん太った。

**教訓**: (1) **ベンチの前提を疑う**: 「16 MiB の 1 打鍵 0.9 ms」は測っていない値だった（打鍵のベンチは空の文書）。6 本目を足したら 16 MiB の 1 鍵は 2.2 ms で、原因は add の複製（#174）ではなく piece を切るたびに本文を走査し直す `clipped` だった（#184）。速さは言う前に測る。(2) 起動の区間は今日この機械で ±30 ms 揺れ、今の main の exe も同じく落ちた。落ちた区間が変更の経路より手前なら対照の exe を同じ時間帯に測って雑音と判断する（gate-proofs 5-ay の末尾）。(3) 実装席の「確認してほしい点」は 5 席で 25 件。ADR を実測に合わせて設計席が直したのは #174（リンク名・引数の束ね）・#176（undo 1 単位は `sealed()` では足りず差分 1 つの `Edit`・入れ子は再帰でなく列の頭へ・打った鍵だけ録る `VimKeySource`・`StoreVimMacro`）・#179（到着幅を欠測の理由にしない）。(4) headless の Vim は録画を観測できない（`:normal!` の中の `q`）。fixture は再生だけ（`register` 欄）、録画は契約。(5) python の heredoc に `"ap` のような二重引用符を入れると bash が壊れる。長い docs の編集は Write でスクリプトを書いてから実行。commit の subject は 100 文字以内（hook で落ちる）。(6) 同じ機械で 3 席が並行してビルドすると速さの計測に雑音が乗る。計測は席が止まってから設計席が回す。

## Issue #175: 本文の Tab を空白 8 個ぶんの tab stop で描く（ADR 0045・実装席 Opus 5.5・1 席・probe は Sonnet）

`SetIncrementalTabStop` の呼び出しが 0 件で、Tab は DirectWrite の既定の tab stop、ADR 0034 の仮想桁（8 桁）と別の規則だった。`create_body_formats` の 1 か所で空白 1 個の advance × 8 を設定。PNG（`a<Tab>b` の `b` と空白 8 個の `b` が同じ x・`rightInk=[187,187,187,187]`）で受理。等幅でない guifont は近似。PR #177・gate-proofs 5-ax。

## Issue #174: add バッファを 64 KiB の chunk の列にする（ADR 0044・実装席 Opus 5.5・1 席・probe は Sonnet）

`replaced` が非空の挿入のたびに add 全体を複製していた（O(打鍵数²)）。add を chunk の列にし、piece は chunk を跨がず（`view_of` は 1 本の `string_view` のまま）、末尾 chunk への追記は「自分の値が先端を持つときだけその場で伸ばす」。fixture 1339 不変・scopes same 21・契約 +15（分岐・70,000 打鍵で piece 2・200 KiB の挿入）。速さは設計席が Release で 4 回（起動の区間の揺れは main の exe も同じ・打鍵は基準内）。クリーンビルドの遅れは無し。PR #178・gate-proofs 5-ay。

## Issue #176: マクロ `q` `@`（ADR 0046・実装席 Opus 5.5・1 席・probe は Sonnet で本物の Vim）

engine が `q{a-z}`（大文字は追記）から `q` までの打った鍵を名前つきの鍵列（26 本）に録り、`@{a-z}` `@@` `[count]@a` は `.` と同じ `VimReplay` で controller が 1 鍵ずつ流す。oracle は録画を観測できない（`:normal!` の中の `q`）ので、fixture は `register` 欄で再生だけ（`macro-*` 20 件・1339 → 1359・既存は逐語不変）、録画→再生の一気通貫は契約 `--vim-macro`（280 checks・probe の `feedkeys` の表が期待値）。実測で設計に足した形 4 つ（差分 1 つの `Edit` で undo 1 単位・入れ子は鍵の列の頭へ差し込み上限 100・`VimKeySource` で打った鍵だけ録る・`StoreVimMacro`）は ADR に取り込んだ。PR #181・gate-proofs 5-az。残る穴: 失敗の印は主な経路だけ・oracle の `q` の拒否は拒みすぎる側。

## Issue #180: 録画中の `recording @a`（実装席 Opus 5.5・1 席）

表示値に `recording` 1 欄、renderer がモード表示の隣に `muted` で描く。PNG で受理。PR #183・gate-proofs 5-ba。

## Issue #179: 16 MiB を開いてから 200 打鍵するベンチ（ADR 0044 決定 6・実装席 Opus 5.5・実装 1 席＋差し戻し 1 席）

`key-to-frame-burst-200-16mib` を 6 本目に。最初の形は到着幅 50 ms の上限で常に欠測になった（16 MiB では到着幅 = 鍵の処理時間そのもの・432〜1758 ms）→ 差し戻しで 16 MiB では上限を使わず内訳に残す。設計席が実機で `--check` 6 本 0 regression → `--adopt --bench` で基準値 427.7 ms（#174 の前の exe は 513.6 ms）。PR #182・gate-proofs 5-bb。

## Issue #184: 改行の索引をバッファごとに 1 本共有し piece は窓だけ持つ（ADR 0047・実装席 Opus 5.5・1 席・probe は Sonnet）

#179 のベンチが出した 16 MiB の 1 鍵 2.2 ms の原因は add の複製ではなく、`clipped` が piece を切るたびに切った側の本文を `newlines_in` で走査し直して改行の索引（20 万個・1.6 MiB）を作り直すこと（1 打鍵に最大 4 回）。索引は original と各 add chunk で 1 本ずつ共有し、piece は窓の添字 `[newline_begin, newline_end)` だけを持つ（切る・伸ばす・数えるは二分探索）。fixture 1359 不変・scopes same 22・契約 +14（200,000 行に 600 回の編集）。実機で `key-to-frame-burst-200-16mib` が 462 ms（対照）→ 7〜9 ms。基準値は ADR 0047 決定 7 のとおり `--adopt --bench --repetitions 15` の中央値 7.4 ms で締めた（5 回の中央値 6.9 ms は上限 8.9 ms で今日の 1 回が落ちる詰めすぎだった）。席は 429 で 1 度止まり、同じ席に「続きを」で再開（commit と Release は残っていた）。PR #185・gate-proofs 5-bc。

## Issue #190: oracle の `q` の拒否を NORMAL / VISUAL のコマンドの位置だけに狭める（実装席 Opus 5.5・1 席・probe は Sonnet で本物の Vim）

`records_a_macro` は検索の外の `q` を一律に拒んでいて `fq` `rq` `iq<Esc>` `@q` `"q` が書けなかった。Sonnet の probe が本物の Vim 9.1 の `-es` で `reg_recording()` を位置ごとに実測し、`f` `r` `@` `"` の直後・INSERT の中・`dq` `diq` は録画を始めず `"aqa` は始めることを確認。「`:normal!` の後に `reg_recording()` を見る」案は対になった `qaxq` が空に戻って見逃すので不採用。oracle 側の保守的な状態機械（NORMAL・オペレータ待ち・INSERT・VISUAL・検索/Ex の入力行）にし、迷う位置は拒む側に倒す。実装席が表から 2 点広げた（VISUAL の `f F t T` の引数・検索行は `<Esc>` でも閉じる）のは両方拒む側なので受理。反例 23 件・fixture 1359 件は正準化でバイト一致。PR #192。

## Issue #191: `EditorController` の `erase().insert()` 3 か所を 1 回の `replaced` に（実装席 Opus 5.5・2 席・probe は Sonnet）

1 席目は `replaced` が `TextBuffer` の private にあって止まった（Issue の「やらないこと」に当たるので判断を仰いだ・正しい止まり方）。設計席の判断 (a) で public にし 2 席目が 3 行を置き換えた。報告の「残るリスク」（`replaced` は範囲を丸めない）を設計席が読み、`insert` / `erase` にあった丸めを `replaced` へ寄せて両者を素通しの口にした（以前と同じ振る舞いを呼び手に依らず保つ）。PR の本文が GIT-001 / GIT-004（`Closes` と必須欄）で 2 回落ちた: 依頼書に PR 本文の型を書いていなかった（#196 で型に足す）。`gh run rerun` は古い event（旧本文）を使うので本文の修正は close / reopen で新しい run を起こす。既定 14009 checks・fixture 1359 不変。PR #194。

## Issue #193: `"a` の接頭辞と名前つきレジスタ（ADR 0048・実装席 Opus 5.5・4 席・probe は Sonnet で本物の Vim）

Sonnet の probe が `-es` と `feedkeys(…, 'xt')`（`'x'` だけでは録画が空になる）で実測: `"ayy` の後の `@a` は末尾の改行が `<NL>` として効く／`qaxq` の後の `"ap` は `x`・`<Esc>` `<CR>` `<Left>` は 0x1b・0x0d・`<80>kl` で literal に入る／`"ayy`（V）へ `qAxq` は `abcdefx\n` で V／`"a"byy` は後勝ち・`"a3yy` と `3"ayy` は同じ・`"a<Esc>` は捨てる／名前つきへ書くと無名も更新・`"_dd` は無名を変えない／`"add` の後の `.` は同じ `"a`・`"Add` の後の `.` は追記を繰り返す／`:normal!` の中で `"ayy` `"ap` `"Ayy` は動く（fixture にできる）。

ADR 0048 の要点: レジスタは a〜z の 26 本の `VimRegister`（本文）の表 1 つでマクロも本文（`VimMacroRegisters` と `StoreVimMacro` は消えた）・`"` の接頭辞（`a-z` `A-Z` `"` `_`）・鍵列 ↔ 本文の写しは core の 1 対 `vim_register_text` / `vim_keys_of_text`（特殊鍵は Vim が typeahead に置くバイト・`<80>` は U+0080 で本文の UTF-8 を守り描画は ADR 0040 の `<80>`）・再生は窓の鍵と同じ口（`deliver_vim_key`・本文の `/ab\r` は入力行を経て確定・再生の中の入力行の `<Esc>` は Vim の `c_<Esc>` どおり確定）・`"` と名前は `.` の記録に残す・NORMAL / VISUAL の `<NL>` `<CR>` `+` `-`。

4 工程（core の表と写し → 再生の口 → `"` の接頭辞と fixture → 決定 8 の残りと PR）。工程 1 は検索を含むマクロが再生の口の変更を待つことを見つけて止まり（赤 10 checks で commit・次の工程で緑）、工程 2 が決定 8 の `<Esc>` の Vim との差を実測で報告し ADR を改訂、工程 3 が `"_p` を E353 と同じ `refused` にして ADR を補正、工程 4 が harness の入力行の写しの複製を controller の 1 口に寄せた。fixture 1359 → 1384（`register-*` 25 件・変更 0）・既定 14009 → 14320 checks・`--vim-macro` 289 → 625（`macro-*` と `register-*` の 45 件を再生）・他 21 scope は same・symbols / conformance 0。見つけた穴: NORMAL の `<Space>` が未実装（fixture の本文を変えて回避）・fixture の記法に `<Left>` 等が無い・録画中に窓で打った入力行の `<Esc>` は取消なので Vim の `/ab<Esc>` は録れない・矩形への追記は `refused`。PR #195。

## 6 回目の区切り（2026-09-23）

main `c98b93c`（#195）。fixture 1384・既定 14320 checks・scope 22・ADR 0048 まで・open な Issue は #196（本 docs）だけ。席は Sonnet の probe 2 席・Opus の実装 7 席（#190 1・#191 2・#193 4）。速さ（設計席が席の停止後に Release `build/release-c98b93c` で実測・`out/speed/2026-09-23T11-44-27Z.json`）: 6 benches / 0 regressions。起動 233 ms・窓 37 ms・1 打鍵 0.90 ms・200 打鍵 3.7 ms・16 MiB を開く 292 ms・16 MiB で 200 打鍵 8.1 ms（1 回だけ 810 ms の外れ値・中央値は基準内）。再生の口の変更（決定 8）は打鍵のベンチに出ていない。

## Issue #198: `VimStep.cpp` の鍵の表と検索関数を `VimKeyTable.hpp/.cpp` に切り出す（ADR 0042 決定 6・実装席 Opus 5.5・1 席・probe は Sonnet）

#193 で表に 5 行足したので、ADR 0042 決定 6 の「次に表を触る Issue で」を本 Issue で行った。Sonnet の probe が表 5 本と検索・検査の関数 7 本が 107〜361 行に連続して収まること・呼び出し元 10 か所・先例（`VimSearch.hpp` / `VimTextObjectRange.hpp` は型を持たない純関数のヘッダ）を確認。`VimKeyTable.hpp` は `vim_action_for` `vim_group_for` `vim_motion_for` `vim_text_object_for` `vim_motion_is_exclusive` の 5 本を公開し、表と `static_assert` は `.cpp` の無名名前空間（内部ヘッダは作らない）。共有の定数 2 つ（`line_feed` `control_r_character`）は両方の `.cpp` に同じ `constexpr`（リテラルの写し）。`VimStep.cpp` は 4141 → 3879 行。fixture 1384・既定 14320 checks・scopes 22 / same 22 は不変。`vim_group_for` の戻り値は「中身を変えない」を優先して `optional` のまま（Issue の見出しと違う点を席が報告・受理）。PR #199。

## Issue #200: `<Space>` `<BS>` は行をまたぐ `l` `h`（ADR 0049・実装席 Opus 5.5・1 席・probe は Sonnet で本物の Vim）

#193 の工程 4 で見つけた穴（NORMAL の `<Space>` が未実装・fixture の記法に `<Space>` と矢印が無い）。Sonnet の probe が oracle と同じ設定で実測: 裸の `<Space>` は行末から 1 打鍵で次の行へ・`d<Space>` は行末でまたがず最後の文字を消す・`d<BS>` は行頭で改行だけ消す・`3d<Space>` は行単位・VISUAL は行末で 1 打鍵「足踏み」。さらに `execute "normal! " . keys` が先頭の空白を食う穴を発見。設計席はこれを Vim の `nv_right` / `nv_left` の 1 つの規則（オペレータ待ちと VISUAL では行末の位置＝改行の上に一度止まる）と `:help exclusive` の 2 規則で説明し、ADR 0049 に書いた。

実装席が Vim で実測して 2 点を補正: `y<BS>` は止まらず前の行の最後の文字へ渡る（`nv_left` の d / c だけの特例）、`d<BS>` `c<BS>` が文字のある行へ渡ったときは exclusive の言い換えを通さない（`CA_NO_ADJ_OP_END`・通すと行頭の `d<BS>` が空の範囲になる）。止まる・渡るは閉じた enum `VimLineEndStop`。`<BS>` は NORMAL で `acted()`、VISUAL で `visual_acted()` を通る。記法に `<Space>` と矢印 4 つ（`KEY_NAMES` と `vim_key_names` の 2 か所）、先頭が空白の `keys` は oracle の `normal_keys()` が `1` を前置（conformance の反例 1 つ）。oracle の測定コードが変わると `--only` が拒むので、oracle の変更を先に commit して `--reuse-ref` にした（#193 と同じ手順・約束に足す）。fixture 1384 → 1423（`space-*` 39 件・矢印 6 件を含む・既存の変更 0）・既定 14320 → 14602 checks・scopes 22 / same 22・symbols / conformance 0。`VimEngineTests.cpp` の「NORMAL / VISUAL の Backspace は何もしない」の旧い前提を外した（範囲外 1 ファイル・受理）。PR #202。

## 数字レジスタとクリップボードの probe（Sonnet・本物の Vim・Issue 未起票）

`out/probes/probe-numreg-2026-09-23.md`（scratchpad の写し）。実測: `"0` は yank だけに入り `"ayy` は入らない／`"1`〜`"9` は 1 行以上の削除・変更で繰り下がり、1 行未満は `"-` だけ（`d/pat<CR>` は両方）・`"add` も `"1` に入る・`"_dd` は全部不変／`"1p` の後の `.` は `"2p` `"3p` と番号が進む（`:help redo-register`）／`"+` `"*` は `-es` でも実機の OS クリップボードを読み書きする（退避・復元して実測）ので fixture にできず、`ClipboardPort`（application の型つき interface・実装は adapters/win32 の 1 か所）のテストダブルで契約だけ／Nib の engine は `VimEditorView` にクリップボードを持たず OS に触れない。設計（ADR）は次のセッション。

## 7 回目の区切り（2026-09-23）

main `e170089`。fixture 1423・既定 14602 checks・scope 22・ADR 0049 まで・`VimStep.cpp` 4022 行・open な Issue は #201（本 docs）だけ。席は Sonnet の probe 3 席（表・`<Space>`・数字レジスタ）・Opus の実装 2 席（#198・#200）。hide の指示「区切りのいいところで止めて日報と引き継ぎ書を書いて」で停止。
