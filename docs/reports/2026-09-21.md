# 日報 — 2026-09-21 / NeNe Nib

## Issue #84: rによる文字置換

統合単位は [PR #86](https://github.com/hideyukiMORI/nene-nib/pull/86)。必須checkはGit規約・検証記録・空白を確認し、成功した対象検証は再利用する。

hideの続行指示でmain `8d7b3307b748b8341c1e6751631a365e0725f596`（#81 / PR #83）から `feat/84-vim-replace-character` で実装した。NORMALの回数指定、文字/行単位VISUALの範囲、Unicode/Tab、NORMALのEnter、取消、レジスタ保持、CRLF、undo/redoが対象。ADR 0029を先に受理し、既存VimInputWaitとVimVisualRange、controllerのreplace/EditHistoryへ接続した。新しいVimReplaceRangeは範囲・LF本文・caretだけを持ち、本文や履歴の所有を増やさない。挿入と置換の改行/caret変換は共通の1本。

固定Vimで41ケースを通常/+Escの82起動で測り、追加probe11起動と合わせて93起動。VISUAL r<Enter>の2件はliteral CRをoracleがLFへ変換してしまう既存問題を発見し、TextBufferの末尾CRの扱いとともに [Issue #85](https://github.com/hideyukiMORI/nene-nib/issues/85)へ分離した。採用39件でfixtureは512→551。既存512行と測定metadataは照合して再利用し、全件は測り直していない。制御文字引用/置換・Ctrl-e/yは後続。

対象51fixtureと入力待ち・取消後続行・保存bytes・undo/redoなど460 checksがすべて成功。初回buildはvisual_actedが61行でtidyに拒否され、検索/g/rの開始処理を既存input_actionにまとめて修正。閾値は変えず、修正後のDebug/tidy/ASan/UBSan build、symbols2libs/0、conformance0、formatが成功。読み取り専用の独立レビューは未解決指摘なし。コマンドと証拠は [gate-proofs 5-u](../quality/gate-proofs.md)。成功結果は関連入力が不変なのでpush/review/mergeで再利用する。

computer-useのsky.list_apps()はnative pipe接続エラー（os error 2）で、今回の実機画面確認は未実施。アプリの起動・入力は送っていない。起動中の旧版PID41000を保ち、成果物を `build/issue84/NeNeNib.exe` へ分離した。SHA-256 `985ee9f578f84613a24b1a21d121721724a95b75865fb10072035ac20134fe07`。CMake出力設定は既定へ復元済み。

規則: FR-003 / ARC-001/004/007/009 / CPP-002/004/006/011/012 / QLT-001/008/012/013 / CNF-010 / GIT-001〜004。ADR 0029・0018補足・用語集・現況・日報・引き継ぎ・検証記録を更新。保存schema・依存・ゲート変更なし。Waivers: none。統合状態はGitHubのIssue/PRが正。

## Issue #81: 行単位VISUAL yankの戻り位置

統合単位は [PR #83](https://github.com/hideyukiMORI/nene-nib/pull/83)。必須checkはGit規約・検証記録・空白を確認し、成功した対象検証は再利用する。

hideの続行指示でmain `3b02f136c41905bb2667ddcf97f04087fcfa5fae`（#77 / PR #82）から `fix/81-visual-yank-caret` を作成。Issue #77で分離した行単位VISUALのyank後の列不一致を修正した。統合状態はGitHubのIssue/PRを正とする。

固定Vimの実測により、下向き・単一行は選択範囲の先頭行の列1、上向きの複数行は現在位置と判明。同じ行で左へ移動した場合も列1になる。字下げの先頭への一律移動ではない。既存の `VimStep.cpp::yanked_caret` だけに判定を加え、NORMALの行単位yank・文字単位yankは維持した。ADR 0018の古い一般化を補正し、型・状態・効果・保存schema・依存は追加していない。

新規測定22件と#77から再利用した1件のfixtureを採用し、489→512件。既存の通常/文字単位yank等10件を併せた対象33fixtureは、修正前265 checks中13失敗、修正後265 checksすべて成功。対象build（Debug、tidy、ASan/UBSan）、symbols2libs/0、conformance0、formatも成功。詳細・実行コマンド・再利用根拠は [gate-proofs 5-t](../quality/gate-proofs.md)。本件は1関数の局所修正なのでroot単体の自己レビューとし、独立レビューは追加していない。

実機操作にはcomputer-useスキルを試したが、`sky.list_apps()`がnative pipeの接続エラー（os error 2）で失敗した。**今回の実機画面確認は未実施**。起動・入力を送っておらず、hideの旧版ウィンドウPID41000は保持している。修正版は `build/issue81/NeNeNib.exe`（SHA-256 `d02d7d3af67b0a89c3821c61a857c121d57815d48aeedf78b72203d26d741c82`）。同じCMake targetの出力先だけを一時分離して作成し、CMake設定は既定へ復元済み。

規則: FR-003 / ARC-001 / ARC-004 / ARC-007 / CPP-002 / CPP-004 / QLT-001 / QLT-008 / QLT-012 / QLT-013 / CNF-010。Waivers: none。dot・全文検索・テキストオブジェクト・一般Ex等は引き続き未実装で、それぞれ別Issueとする。成功済み検証は文書追記・push・レビュー・mergeでも再利用する。
