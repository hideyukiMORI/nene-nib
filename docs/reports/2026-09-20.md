# 日報 — 2026-09-20 / NeNe Nib

記録者: NeNe Nibサナ。hide の続行指示で C2（Issue #60）を進め、途中で検証方針の更新（#61・PR #62）を先に統合した。調査・設計・実装・検証は単体、必要な独立コードレビューだけ読み取り専用で依頼した。

## C2 の変更

設定を `EditorState` が所有し、`SettingsPort` と既存 `FilePort` の原子的置換で `%LOCALAPPDATA%/NeNeNib/settings.v1` に保存する（ADR 0020）。テーマ・フォント名・pt の4キー版付き形式。読込失敗・未知版・ロック・他プロセスの更新を型で返し、壊れた設定や競合相手の更新を潰さない。保存に成功してから表示を変える。

Ctrl+拡大/縮小/既定復帰、テンキー、Ctrl+ホイールを通常・Vim に接続した。本文の既定は従来の13.5 DIPから仕様の13.5 pt＝18 DIPへ訂正し、行高は29 DIP。行番号の欄・文字・余白も同じ比率で拡縮し、タイトルとステータスは固定。描画・クリック・表示行数・IMEは同じ設定から導く。無変更のサイズ操作ではスクロール位置を保持する。

独立レビューでは、設定の端のASCII空白（VT/FF）と、OSテーマ・フォント比例係数の重複経路を修正した。単体3,992 checks、adapter109 checks、分岐1,454/1,588（91.56%）。実機は120 DPIで拡縮、再起動、クリック、日本語変換/確定、無変更のスクロール保持が成功。起動・打鍵4指標の中央値は209.147 / 31.7409 / 0.803 / 2.419 ms、退行0・欠測0。詳細は [検証記録](../quality/gate-proofs.md) の Issue #60 節。

## 検証方針と次

ADR 0021 により、全件ゲートを既定にせず、変更と直接依存に必要な検証を選び成功結果を再利用する。C2でもフルゲート・既存Vim oracle再生成・無関係な16 MiBファイル性能は実行していない。CIはGit規約と検証記録と空白だけを確認する。

C2だけでは FR-016 / FR-017 全体は未完了。次は C3 の Ex入力・`:colorscheme`・`:set fontsize` / `guifont`、その後 Ctrl+P の共通一覧。利用者テーマは C4、矩形選択・ドラッグは別 Issue。統合状態は GitHub を正とする。Waivers: none。

## C3a の進捗（Issue #64）

C2はPR #63でmainへ統合。その後、採用済みEx案AでNORMALの `:`、UTF-8入力編集/Tab補完、`:colorscheme`とフォント設定を実装した。本文・undo・レジスタと入力状態を分離し、相対サイズ変更も含め一つの `persist_settings` へ集約。設定ファイルの形式は変えていない（ADR 0022）。

独立レビューでpipeの誤受理、AltGr、本文外クリック、結果表示中のhidden toggleを修正した。境界検査でstd::formatのlocale参照を検出し、coreは検証済み文字列の連結へ変更。固定STLの不変な数値表と純走査3シンボルのみ許可を明示した。

対象unit153 checks、codec23 checks、symbols違反0、native120 DPIで入力/補完/設定/再起動/本文保持と追加guardが成功。起動/打鍵4指標は206.5191 / 31.8138 / 1.157 / 2.463 ms、退行0・欠測0。全件・無関係な検査は追加していない。詳細はgate-proofs 5-l。次はC3bのCtrl+P共通一覧。一般ExやCtrl+P全体を完了扱いにしない。

## C3b の進捗（Issue #66）

C3aはPR #65で統合後、採用済みCtrl+P案へ設定一覧を接続した（ADR 0023）。候補はExと共通の正本から導き、部分列で絞り込む。フォント候補は入力を補って値入力後に実行。`CommandInput`の和型でEx/一覧の所有を一本化し、通常とVimの各モードから同じ設定評価・保存を使う。選択/本文/undo/count/pendingを保持し、IMEは一覧中に閉じて元へ戻す。

対象単体はCtrl+P130 checksとEx153 checks。nativeは120 DPIで候補・テーマ・フォント・クリック反映・取消・元モード/選択/IME・狭窓・長入力・undo・再起動が成功。独立レビューの未知テーマのエラー不一致を修正し、そのunitとnativeだけ追加確認した。件数表示も実画面で確認した。旧codec/ファイル競合/Vim oracle/無関係なGUIの結果は再利用し、全件検証はしていない。詳細はgate-proofs 5-m。

Ctrl+Pのファイル/履歴等の統合、利用者テーマC4、専用フォント選択UIは引き続き未実装。保存schemaとruntime依存は変更なし、Waivers: none。

## C4a の進捗（Issue #68）

C3bはPR #67で統合した。続いて利用者テーマをC4a（形式/所有/読込）とC4b（カタログ/設定/一覧接続）へ分け、ADR 0024を実装。ファイル由来の文字列はThemeDocumentが所有し、既存Themeは一時値から借りられない。本文16色は必須、UIは既存derive_uiと任意override。名前・版・UTF-8・16KiB・キー・色・出典・コントラストを厳密に検証する。

独立レビューでbasenameと期待名の一致をcaller任せにしていた点を修正。`load_theme(FilePort, FilePath)` が正規ファイル名を検証して期待名を導き、追加のファイル読込19 checksで確認した。修正後のレビューは追加指摘なし。

設定とテーマの行分割はKeyValueFieldsへ共通化し、既存SettingsCodecの契約は維持した。テーマ127 checks、直接影響する設定codec23 checks、純coreの名前/所有21 checksが成功。symbols/conformance違反0。アプリ画面/性能/Vimの経路は変えず、無関係な検証は実行していない。形式説明と例も追加した。C4bの選択接続は未実装、Waivers: none。

## C4b の進捗（Issue #70）

C4aをPR #69で統合し、利用者テーマの選択/保存/復元をADR 0025で接続。共通の不変カタログを設定・Ex・Ctrl+Pが使う。欠落/破損は名前付きで通知し、元settingsの上書きを禁止する。起動時一度の読込で、128件と16KiBの上限、非再帰、途中列挙失敗時の全廃を明確にした。

対象unit88、直接依存Ex153 / palette130 / 設定codec23、Win32 catalog301 checks成功。native120 DPIで独自配色・補完・保存/復元・本文不変・破損保護・初期文書付きnoticeを確認。必要な独立レビューでrvalue借用、notice寿命、blocked時の同値成功表示を修正した。最終レビューの追加指摘なし。検証と再利用の根拠はgate-proofs 5-o。全件検証は実行していない。

保存形式とruntime依存は不変、Waivers: none。次候補はVimの行内文字検索 f/F/t/T と ;/,。Ctrl+P全体や一般Exまで完了とは扱わない。

C4bの起動/打鍵4指標も5試行で退行0・欠測0。初回描画209.9973ms、窓32.813ms、単打鍵1.14ms、200打鍵2.627ms（通常の空profile）。証拠はout/speed/issue70-selected.json、gate-proofs 5-o。
